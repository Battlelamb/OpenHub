---
phase: 05
slice: 05-01
name: Initial GSD Plan
status: complete
wave: release-readiness
updated: 2026-05-31T07:49:37Z
---

# Phase 05 — Initial GSD Plan

## Decision

Proceed with OpenHub through a GSD-style execution loop using Claude Code Opus as the preferred implementation runtime.

## Slice 05-01: Make the GSD Runtime Reproducible

### Goal

Commit the local GSD integration and project rules so future Claude Code / Hermes sessions start with the same operating contract.

### Tasks

- [x] Install GSD locally for Claude Code under `.claude/`.
- [x] Install GSD locally for Hermes Agent under `.hermes/`.
- [x] Configure GSD/OpenHub model defaults to Anthropic Claude Opus 4.7.
- [x] Add OpenHub Claude Code operating contract to `CLAUDE.md`.
- [x] Record credential state and next-phase constraints in `05-CONTEXT.md`.
- [ ] Validate Claude Code execution once credentials are actually available.

### Verification

- `npx get-shit-done-cc@latest --claude --hermes --local --profile=core --portable-hooks` completed successfully.
- `gsd-sdk --help` returns CLI usage.
- `.claude/get-shit-done/bin/shared/model-catalog.json` maps Anthropic `opus` to `claude-opus-4-7`.
- `claude auth status` currently reports not logged in; execution remains gated until credential source is present.

## Slice 05-02: Release-Readiness Snapshot

### Goal

Before feature work, produce a fresh, evidence-backed state snapshot.

### Tasks

- [ ] Backend: run the smallest reliable pytest subset first; then expand.
- [ ] Frontend: run `npm run typecheck` and `npm run build` in `web/`.
- [ ] Runtime: check local/public API health if services are running.
- [ ] Docs: compare README/roadmap/planning with current implementation.
- [ ] OpenHub coordination: identify stale tasks, evidence gaps, and launch blockers.

## Slice 05-03: First Product Slice Candidate

Preferred candidate after snapshot: **Stuck Work Recovery UX**.

Reason: it directly supports the OpenHub promise — durable, visible, verifiable multi-agent work — and matches both Gastown and GSD benchmark lessons without changing architecture.

Potential scope:

- Detect stale claimed/running tasks with expired leases.
- Surface them in API/dashboard with clear status copy.
- Add admin recovery action guarded by tests.
- Add evidence/audit trail for recovery.
