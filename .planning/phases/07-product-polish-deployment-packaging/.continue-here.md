---
context: phase
phase: 07-product-polish-deployment-packaging
task: 6
total_tasks: 6
status: paused
last_updated: 2026-05-26T06:52:03.134Z
---

# Continue Here — Phase 07 Product Polish + Deployment Packaging

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Raw GSD progress as final truth | `gsd-tools progress table` can undercount super-plan phases without SUMMARY.md files and report planned even when curated STATE/ROADMAP are current. | advisory | Read `.planning/STATE.md`, `.planning/ROADMAP.md`, and current phase evidence; report raw artifact telemetry separately if it differs. |
| Tagging before full gate evidence | 07-05 only documented runtime ops; it did not run the full release gate. | advisory | Do not tag or claim release readiness until 07-06 verification evidence exists. |

<current_state>
Phase 07 is paused after completing 07-05 Runtime ops cleanup docs. The repo was clean and pushed at `c84108819af05462add2bd02bebe5ae6054b858b` before this handoff was created. Current curated planning truth says the next executable slice is 07-06 Full verification + tag decision.
</current_state>

<completed_work>
Completed Tasks:
- 07-01 Dashboard truth audit — done.
- 07-02 Dashboard truth fixes — done.
- 07-03 Deployment packaging smoke — done.
- 07-04 Test/CI command alignment — done early.
- 07-05 Runtime ops cleanup docs — done and pushed as `c841088`.

07-05 deliverables:
- `docs/OPERATIONS.md` created with active services, disabled legacy bridge, Cloudflare Tunnel route, secret-safe diagnostics, env permissions, recovery checks, and release-verification reminder.
- `.planning/phases/07-product-polish-deployment-packaging/07-05-RUNTIME-OPS.md` created with evidence.
- README now links to the operations runbook.
- `.planning/STATE.md`, `.planning/ROADMAP.md`, and `07-PLAN.md` updated to mark 07-05 complete and 07-06 next.
</completed_work>

<remaining_work>
- 07-06 Full verification + tag decision — not started.
  - Run backend tests.
  - Run frontend tests.
  - Run frontend build.
  - Run Playwright E2E.
  - Smoke live health, ACN, and dashboard routes.
  - Record evidence in `07-06-VERIFICATION.md`.
  - Decide whether to update `CHANGELOG.md` and tag.
  - Update `.planning/STATE.md`.
  - Commit, push, and verify local/origin/remote HEAD equality.
</remaining_work>

<decisions_made>
- Use repo-local compatibility validation for GSD Redux: `node .codex/get-shit-done/bin/gsd-tools.cjs validate health` and `node .codex/get-shit-done/bin/gsd-tools.cjs validate consistency`.
- Do not use `gsd-sdk validate health`; current `gsd-sdk` rejects direct `validate` subcommands and expects `run`, `auto`, `init`, or `query`.
- Treat Claude Opus 4.7 as the planning/research/high-level reasoning surface and GPT 5.5 via Codex as the implementation/execution-worker surface.
</decisions_made>

<blockers>
- None blocking.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current curated project state and next slice.
2. `.planning/ROADMAP.md` — Phase 07 slice status and success criteria.
3. `.planning/phases/07-product-polish-deployment-packaging/07-PLAN.md` — exact 07-06 verification plan.
4. `.planning/phases/07-product-polish-deployment-packaging/07-05-RUNTIME-OPS.md` — latest runtime ops evidence.
5. `docs/OPERATIONS.md` — active service names, diagnostics, and recovery checks.
6. `.gsdrc.toml` — current verification command list.

## Infrastructure State

- `openhub-api.service`: active during pause-work snapshot.
- `openhub-bridge-brunhilde.service`: active during pause-work snapshot.
- `openhub-bridge.service`: expected disabled/inactive legacy bridge; documented in `docs/OPERATIONS.md`.
- Local `/v1/health/simple`: HTTP 200 during pause-work snapshot.
- Local `/v1/acn/status`: HTTP 200 during pause-work snapshot; ACN payload reported 5 agents.
- Public `hub.brunhilde.cloud`: runtime docs from 07-05 verify Cloudflare route to `127.0.0.1:7788`.

## Observations / Follow-ups

- API logs showed repeated `heartbeat_check_failed` entries with `can't compare offset-naive and offset-aware datetimes` during 07-05. Health and ACN remained HTTP 200. Treat this as a 07-06 release-verification follow-up if it persists.
- Active Phase 07 placeholder scan only found intentional `.env.example` / checked-in placeholder wording in deployment smoke notes, not a current incomplete 07-06 artifact.

## Pre-Execution Critique Required

- Not required before resuming. 07-06 is a verification/evidence slice, not a new design-to-execution transition.

<context>
The work paused at a clean boundary: 07-05 is complete and pushed; 07-06 is the next and final Phase 07 gate. The next agent should be evidence-first and should not skip E2E/live smoke or tag decision recording.
</context>

<next_action>
Start with: run the 07-06 verification commands from `07-PLAN.md`, beginning with backend tests: `. .venv/bin/activate && python -m pytest tests/ -x -q --tb=short`.
</next_action>
