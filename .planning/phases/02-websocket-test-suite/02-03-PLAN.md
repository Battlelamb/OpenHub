---
phase: 02-websocket-test-suite
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/unit/test_capability_matcher.py
  - tests/integration/test_task_lifecycle.py
  - tests/integration/test_agent_lifecycle.py
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "Capability matcher returns highest-scoring agent when multiple agents have overlapping capabilities"
    - "Capability matcher returns empty result when no agents match required capabilities"
    - "A task can be created, claimed, started, completed through the full HTTP lifecycle"
    - "A failed task with retries remaining can be retried"
    - "An agent can register, send heartbeats, and be detected as offline when heartbeats stop"
  artifacts:
    - path: "tests/unit/test_capability_matcher.py"
      provides: "TEST-02 capability matching tests"
      min_lines: 60
    - path: "tests/integration/test_task_lifecycle.py"
      provides: "TEST-03 task lifecycle integration tests"
      min_lines: 80
    - path: "tests/integration/test_agent_lifecycle.py"
      provides: "TEST-04 agent registration and heartbeat tests"
      min_lines: 60
  key_links:
    - from: "tests/unit/test_capability_matcher.py"
      to: "app/services/capability_matcher.py"
      via: "direct CapabilityMatcher instantiation"
      pattern: "CapabilityMatcher"
    - from: "tests/integration/test_task_lifecycle.py"
      to: "/v1/tasks"
      via: "TestClient HTTP calls"
      pattern: "test_client.post.*tasks"
    - from: "tests/integration/test_agent_lifecycle.py"
      to: "/v1/agents"
      via: "TestClient HTTP calls"
      pattern: "test_client.post.*agents"
---

<objective>
Write unit tests for capability matching (TEST-02) and integration tests for task lifecycle (TEST-03) and agent registration/heartbeat (TEST-04).

Purpose: These three test sets exercise the core backend services that already exist. They must all use real in-memory SQLite per D-16 (no mocked DB). Tests run through the TestClient HTTP layer for integration tests, and directly against service classes for unit tests.

Output: Three new test files covering TEST-02, TEST-03, TEST-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-websocket-test-suite/02-CONTEXT.md
@.planning/phases/02-websocket-test-suite/02-RESEARCH.md
@.planning/phases/02-websocket-test-suite/02-01-SUMMARY.md

<interfaces>
<!-- Services under test -->

From app/services/capability_matcher.py:
```python
class CapabilityMatcher:
    def __init__(self, database):
        ...
    def find_best_agent(self, required_capabilities: List[str]) -> Optional[CapabilityMatch]
    # Returns: CapabilityMatch with match_score, matched_capabilities, missing_capabilities, confidence_score
```

From app/services/task_service.py:
```python
class TaskService:
    def __init__(self, database):
        ...
    def create_task(self, task_data: TaskCreate, created_by=None) -> Task
    def claim_task(self, task_id: str, claim_data: TaskClaim) -> bool
    def start_task(self, task_id: str, agent_id: str) -> bool
    def complete_task(self, task_id: str, agent_id: str, completion: TaskComplete) -> bool
    def fail_task(self, task_id: str, agent_id: str, failure: TaskFail) -> bool
```

From app/services/agent_service.py:
```python
class AgentService:
    def __init__(self, database):
        ...
    def register_agent(self, agent_data: AgentCreate) -> Agent
```

From app/services/heartbeat_service.py:
```python
class HeartbeatService:
    def __init__(self, database):
        ...
    async def update_agent_status(self, agent_id, status)
```

<!-- API endpoints (for integration tests) -->
POST /v1/agents/register - register agent
POST /v1/agents/{id}/heartbeat - send heartbeat
POST /v1/tasks - create task
POST /v1/tasks/{id}/claim - claim task
POST /v1/tasks/{id}/start - start task
POST /v1/tasks/{id}/complete - complete task
POST /v1/tasks/{id}/fail - fail task
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write capability matcher unit tests (TEST-02)</name>
  <files>tests/unit/test_capability_matcher.py</files>
  <read_first>
    - app/services/capability_matcher.py
    - app/models/agents.py
    - app/database/repositories/agents.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - test_exact_match_single_capability: agent with ["python"] matches task requiring ["python"], score is 1.0
    - test_exact_match_multiple: agent with ["python", "testing", "docker"] matches ["python", "testing"], score is 1.0
    - test_partial_match: agent with ["python"] partially matches ["python", "rust"], score between 0 and 1
    - test_no_match: agent with ["python"] does not match ["rust", "go"], returns None or score 0
    - test_best_agent_selection: two agents with different capabilities, matcher returns the one with higher score
    - test_empty_capabilities: agent with empty capabilities list matches nothing
    - test_fuzzy_match_case_insensitive: "Python" matches "python" (if supported, else document behavior)
  </behavior>
  <action>
Create tests/unit/test_capability_matcher.py.

Read the CapabilityMatcher source first to understand exact method signatures and return types.

Use `test_client` fixture to ensure DB is initialized (lifespan runs). Then:
1. Get database: `from app.database.connection import get_database; db = get_database()`
2. Register test agents via AgentService with different capability sets
3. Instantiate CapabilityMatcher(db) and call find_best_agent() with various requirement lists
4. Assert on match_score, matched_capabilities, missing_capabilities

Use synchronous test functions (CapabilityMatcher is sync). If the matcher needs agents in the DB, register them via the AgentService or directly via the repository in a setup fixture.

Important: The test_client fixture triggers app lifespan which initializes the DB. Import and use it even if not making HTTP calls, to ensure tables exist.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_capability_matcher.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" tests/unit/test_capability_matcher.py returns at least 5
    - grep "CapabilityMatcher" tests/unit/test_capability_matcher.py returns at least 2 matches
    - grep "match_score\|matched_capabilities" tests/unit/test_capability_matcher.py returns at least 1 match
    - All tests pass
  </acceptance_criteria>
  <done>TEST-02 complete: capability matcher tested for exact match, partial match, no match, best agent selection, edge cases</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write task lifecycle integration tests (TEST-03) and agent lifecycle tests (TEST-04)</name>
  <files>tests/integration/test_task_lifecycle.py, tests/integration/test_agent_lifecycle.py</files>
  <read_first>
    - app/api/routes_tasks.py
    - app/api/routes_agents.py
    - app/models/tasks.py
    - app/models/agents.py
    - tests/conftest.py
  </read_first>
  <behavior>
    For test_task_lifecycle.py (TEST-03):
    - test_create_task: POST /v1/tasks with valid payload returns 201 with task id
    - test_create_task_missing_fields: POST /v1/tasks without required fields returns 422
    - test_claim_task: create task then POST /v1/tasks/{id}/claim returns success
    - test_start_task: create+claim then POST /v1/tasks/{id}/start returns success
    - test_complete_task: full lifecycle create->claim->start->complete, final status is COMPLETED
    - test_fail_task: create->claim->start->fail, final status is FAILED
    - test_claim_already_claimed: claiming an already-claimed task returns conflict error

    For test_agent_lifecycle.py (TEST-04):
    - test_register_agent: POST /v1/agents/register with name and capabilities returns 201
    - test_register_duplicate_name: registering same agent name twice returns conflict
    - test_agent_heartbeat: register then POST heartbeat, agent stays online
    - test_agent_list: register multiple agents, GET /v1/agents returns them
    - test_agent_status: registered agent shows "online" status
  </behavior>
  <action>
Create both test files using test_client and admin_headers fixtures from conftest.py.

For test_task_lifecycle.py:
- All tests use `test_client.post("/v1/tasks", json={...}, headers=admin_headers)` pattern
- Read routes_tasks.py first to confirm exact endpoint paths and request body shapes
- The task create payload needs at minimum: title, task_type. Check the TaskCreate model for required fields.
- For claim: need an agent_id. Either register an agent first or use a fixture. Check TaskClaim model.
- For complete: check TaskComplete model for required fields (likely result dict).
- For fail: check TaskFail model for required fields (likely error message).

For test_agent_lifecycle.py:
- Read routes_agents.py to confirm register endpoint path and request body shape
- Check AgentCreate model for required fields (likely name, capabilities list)
- Heartbeat endpoint path: check routes_agents.py
- Agent names must be unique per test to avoid cross-test conflicts. Use UUIDs or timestamps in names.

Use synchronous test functions with TestClient (not async).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/test_task_lifecycle.py tests/integration/test_agent_lifecycle.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def test_" tests/integration/test_task_lifecycle.py returns at least 5
    - grep -c "def test_" tests/integration/test_agent_lifecycle.py returns at least 4
    - grep "/v1/tasks" tests/integration/test_task_lifecycle.py returns at least 3 matches
    - grep "/v1/agents" tests/integration/test_agent_lifecycle.py returns at least 2 matches
    - All tests pass
  </acceptance_criteria>
  <done>TEST-03 complete: task lifecycle (create, claim, start, complete, fail) tested via HTTP. TEST-04 complete: agent registration, heartbeat, listing tested via HTTP.</done>
</task>

</tasks>

<verification>
.venv/bin/python -m pytest tests/ -x -q --tb=short
All tests pass including new capability matcher, task lifecycle, and agent lifecycle tests.
</verification>

<success_criteria>
- tests/unit/test_capability_matcher.py has 5+ tests covering exact/partial/no match scenarios
- tests/integration/test_task_lifecycle.py has 5+ tests covering full task lifecycle via HTTP
- tests/integration/test_agent_lifecycle.py has 4+ tests covering agent registration and heartbeat
- All tests pass with real in-memory SQLite (no mocks)
</success_criteria>

<output>
After completion, create `.planning/phases/02-websocket-test-suite/02-03-SUMMARY.md`
</output>
