# Continue Here — Phase 09 ANP Compatibility Spike

Updated: 2026-05-31T11:23:28Z

## Current status

Phase 09 is planned. No implementation has started.

Plan file:

- `.planning/phases/09-anp-compatibility/09-PLAN.md`

## Goal

Expose a public-safe ANP compatibility surface for OpenHub:

- `GET /.well-known/agent-descriptions`
- `GET /v1/anp/agents/{agent_id}/ad.json`

Keep OpenHub ACN identity, `inv_...` invites, `oh_...` keys, task routing, evidence bundles, and review gates authoritative.

## Start here

Begin Task 09-01:

1. Create `docs/ANP_COMPATIBILITY.md`.
2. Create `.planning/phases/09-anp-compatibility/09-01-ANP-MAPPING-DESIGN.md`.
3. Run:
   - `python scripts/check_dependency_drift.py`
   - `node .codex/get-shit-done/bin/gsd-tools.cjs validate health`
   - `node .codex/get-shit-done/bin/gsd-tools.cjs validate consistency`
4. Commit `docs: plan ANP compatibility surface`.

## Critical guardrails

- Default private: only agents with `labels.anp_public == "true"`, `metadata.anp_public == true`, or `metadata.public == true` appear publicly.
- Never return raw labels/metadata wholesale.
- Never expose keys, tokens, secrets, admin values, bearer strings, IPs, hostnames, workspace paths, or OS details.
- Do not add `did:wba` claims unless the DID is explicitly present in agent metadata.
- No release/tag/publish action unless the operator explicitly chooses a version and target.
