# Continue Here — Phase 09 ANP Compatibility Spike

Updated: 2026-05-31T11:45:54Z

## Current status

Slice 09-01 is complete. Phase 09 is now in progress.

Completed:

- `docs/ANP_COMPATIBILITY.md`
- `.planning/phases/09-anp-compatibility/09-01-ANP-MAPPING-DESIGN.md`

Plan file:

- `.planning/phases/09-anp-compatibility/09-PLAN.md`

## Resume here

Begin Task 09-02 — Serializer service.

Objective: convert an `Agent` model into safe ANP JSON-LD without adding routes yet.

Files to create:

- `app/models/anp.py`
- `app/services/anp_compatibility_service.py`
- `tests/unit/test_anp_compatibility_service.py`

Start with failing tests:

- `test_public_flag_accepts_anp_public_label`
- `test_public_flag_accepts_metadata_boolean`
- `test_private_agent_is_not_public_by_default`
- `test_agent_description_omits_sensitive_metadata`
- `test_agent_description_includes_did_only_when_explicit`
- `test_discovery_page_paginates_public_agents`

Run:

```bash
cd /home/brunhilde/OpenHub
.venv/bin/python -m pytest tests/unit/test_anp_compatibility_service.py -q --tb=short
```

Expected first result: fail because the test file/module does not exist yet.

Then implement the minimal pure mapping functions:

- `is_anp_public(agent: Agent) -> bool`
- `build_agent_description(agent: Agent, base_url: str) -> dict`
- `build_discovery_page(agents: list[Agent], base_url: str, page: int, page_size: int) -> dict`

## Critical guardrails

- Default private.
- Never return raw labels/metadata wholesale.
- Never expose keys, tokens, secrets, admin values, bearer strings, IPs, hostnames, workspace paths, OS details, local paths, process IDs, env values, or bridge command lines.
- Do not synthesize DID values.
- Keep 09-02 pure/service-level: no FastAPI routes yet.
- No release/tag/publish action unless the operator explicitly chooses a version and target.

## Last local validation for 09-01

```text
.venv/bin/python scripts/check_dependency_drift.py
→ passed

node .codex/get-shit-done/bin/gsd-tools.cjs validate health
→ healthy; 0 errors; 0 warnings; one expected open-phase info entry

node .codex/get-shit-done/bin/gsd-tools.cjs validate consistency
→ passed; 0 errors; 0 warnings

git diff --check
→ passed
```
