---
phase: 03-vector-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0003_vector_columns.py
  - app/config.py
  - requirements.txt
  - app/database/vector_availability.py
  - tests/conftest.py
  - tests/unit/test_vector_migration.py
  - tests/unit/test_vector_deps.py
  - tests/unit/test_embedding_service.py
  - tests/unit/test_vector_feature_flag.py
  - tests/unit/test_auto_index.py
  - tests/unit/test_retry_worker.py
  - tests/integration/test_vector_storage.py
  - tests/integration/test_auto_indexing.py
  - tests/integration/test_vector_search.py
  - tests/integration/test_search_api.py
  - pyproject.toml
autonomous: true
requirements:
  - VEC-01
  - VEC-06
user_setup: []
must_haves:
  truths:
    - "Alembic upgrade head adds embedding columns to shared_memory, tasks, artifacts, messages without error on local SQLite"
    - "App starts normally on local SQLite after migration (no crashes from libsql_vector_idx)"
    - "zvec package is absent from requirements.txt and app.config.Settings"
    - "Pytest 'turso' marker is registered and tests marked @pytest.mark.turso are auto-skipped when credentials absent"
    - "Wave 0 test stub files exist with at least one test each (xfail or placeholder)"
  artifacts:
    - path: "alembic/versions/0003_vector_columns.py"
      provides: "Migration adding embedding/embedding_model/embedding_status/embedding_error/embedded_at columns + DiskANN index (guarded)"
      contains: "F32_BLOB(384)"
    - path: "app/database/vector_availability.py"
      provides: "is_vector_enabled() + require_vector() FastAPI dependency returning RFC 7807 503"
      exports: ["is_vector_enabled", "require_vector"]
    - path: "app/config.py"
      provides: "embedding_provider, openai_api_key, vector_search_enabled settings; zvec_path removed"
      contains: "embedding_provider"
    - path: "tests/conftest.py"
      provides: "pytest.mark.turso registration + auto-skip when TURSO_DATABASE_URL unset + mock_embedding_backend fixture"
      contains: "turso"
  key_links:
    - from: "alembic/versions/0003_vector_columns.py"
      to: "shared_memory, tasks, artifacts, messages tables"
      via: "ALTER TABLE ADD COLUMN embedding F32_BLOB(384)"
      pattern: "F32_BLOB"
    - from: "app/database/vector_availability.py"
      to: "app/config.py vector_search_enabled + app/database/connection.py Database._use_turso"
      via: "runtime predicate"
      pattern: "_use_turso"
    - from: "requirements.txt"
      to: "package installs"
      via: "removal of zvec line"
      pattern: "^zvec"
---

<objective>
Lay the foundation for Phase 3: database migration adding vector columns, configuration and dependency cleanup (remove zvec per VEC-01), the vector availability predicate used by every downstream plan, and all Wave 0 test scaffolds required by VALIDATION.md.

Purpose: Downstream plans (02-06) assume migration is in place, zvec is gone, and test files exist to write against. This plan unblocks parallel work on services (Plan 02, 03) in Wave 2.
Output: Migration file, updated config/requirements, vector_availability module, and 10 test stub files registered with the pytest turso marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-vector-database/03-CONTEXT.md
@.planning/phases/03-vector-database/03-RESEARCH.md
@.planning/phases/03-vector-database/03-VALIDATION.md

@app/config.py
@app/database/connection.py
@alembic/versions/0001_initial_schema.py
@requirements.txt
@tests/conftest.py

<interfaces>
From app/config.py (current, to modify):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="AGENTHUB_", ...)
    zvec_path: str = Field(default="./data/zvec", ...)  # REMOVE
    vector_batch_size: int = Field(default=1000, ...)   # KEEP
    embedding_model: str = Field(default="sentence-transformers/all-MiniLM-L6-v2", ...)  # KEEP
```

From app/database/connection.py:
```python
class Database:
    _use_turso: bool  # Set in __init__ based on TURSO_DATABASE_URL + TURSO_AUTH_TOKEN env vars
def get_database() -> Database: ...  # Singleton accessor
```

New settings to add (per D-01, D-22):
- `embedding_provider: Literal["local","openai"] = "local"` (env: AGENTHUB_EMBEDDING_PROVIDER)
- `openai_api_key: Optional[str] = None` (env: AGENTHUB_OPENAI_API_KEY)
- `vector_search_enabled: Optional[bool] = None` (env: AGENTHUB_VECTOR_SEARCH_ENABLED; None means auto = True if Turso configured)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic migration 0003 + config and dependency cleanup</name>
  <files>
    alembic/versions/0003_vector_columns.py,
    app/config.py,
    requirements.txt,
    app/database/vector_availability.py,
    tests/unit/test_vector_migration.py,
    tests/unit/test_vector_deps.py,
    tests/unit/test_vector_feature_flag.py
  </files>
  <read_first>
    - app/config.py (full file, current Settings class)
    - app/database/connection.py (understand Database._use_turso flag and get_database() singleton)
    - alembic/versions/0001_initial_schema.py (migration pattern: revision/down_revision/op.execute)
    - alembic/versions/0002_add_source_to_pending_applications.py (latest revision number to chain from)
    - requirements.txt (find zvec==0.1.0 line to remove)
    - app/middleware.py (RFC 7807 ProblemDetail shape used elsewhere)
  </read_first>
  <behavior>
    - test_vector_migration.py::test_upgrade_adds_columns: runs `alembic upgrade head` against a tempfile SQLite DB and asserts PRAGMA table_info shows `embedding`, `embedding_model`, `embedding_status`, `embedding_error`, `embedded_at` columns on all 4 target tables (shared_memory, tasks, artifacts, messages)
    - test_vector_migration.py::test_upgrade_idempotent: running upgrade twice does not raise
    - test_vector_migration.py::test_index_skipped_on_sqlite: local SQLite upgrade logs a warning for each table's libsql_vector_idx attempt but does not raise
    - test_vector_deps.py::test_zvec_not_in_requirements: reads requirements.txt and asserts no line starts with "zvec"
    - test_vector_deps.py::test_zvec_not_in_settings: `from app.config import Settings; assert not hasattr(Settings(), 'zvec_path')`
    - test_vector_feature_flag.py::test_is_vector_enabled_false_on_sqlite: with no Turso env vars, `is_vector_enabled()` returns False
    - test_vector_feature_flag.py::test_require_vector_raises_503: calling `require_vector()` with no Turso raises HTTPException with status_code=503 and RFC 7807 detail containing "vector search requires Turso"
    - test_vector_feature_flag.py::test_feature_flag_explicit_false_overrides: setting AGENTHUB_VECTOR_SEARCH_ENABLED=false disables even with Turso present
  </behavior>
  <action>
    Step A - Create `alembic/versions/0003_vector_columns.py`:
      - revision = '0003', down_revision = '0002'
      - Constant `VEC_DIM = 384` (matches sentence-transformers/all-MiniLM-L6-v2 per D-01)
      - `upgrade()` loops over `["shared_memory", "tasks", "artifacts", "messages"]` and runs:
        * `ALTER TABLE {t} ADD COLUMN embedding F32_BLOB({VEC_DIM})`
        * `ALTER TABLE {t} ADD COLUMN embedding_model TEXT`
        * `ALTER TABLE {t} ADD COLUMN embedding_status TEXT`
        * `ALTER TABLE {t} ADD COLUMN embedding_error TEXT`
        * `ALTER TABLE {t} ADD COLUMN embedded_at TIMESTAMP`
      - Wrap each ALTER in try/except sqlalchemy.exc.OperationalError to tolerate "duplicate column" on re-runs (idempotency)
      - Second loop creates DiskANN index inside try/except, logging `vector_index_skip table={t} reason={e}` on failure:
        * `CREATE INDEX IF NOT EXISTS idx_{t}_embedding ON {t}(libsql_vector_idx(embedding, 'metric=cosine'))`
      - `downgrade()` drops the 4 indexes via DROP INDEX IF EXISTS (do not attempt DROP COLUMN - SQLite limitation)
      - Per CLAUDE.md: no em dashes in docstrings or comments

    Step B - Edit `app/config.py` Settings class:
      - DELETE `zvec_path` field entirely
      - ADD `embedding_provider: str = Field(default="local", description="Embedding backend: local or openai")`
      - ADD `openai_api_key: Optional[str] = Field(default=None, description="OpenAI API key for embeddings")`
      - ADD `vector_search_enabled: Optional[bool] = Field(default=None, description="None=auto-detect from Turso, True/False=explicit override")`
      - Import `Optional` from `typing` if not already imported
      - Keep existing `embedding_model` and `vector_batch_size` fields

    Step C - Edit `requirements.txt`:
      - Remove the line `zvec==0.1.0` entirely
      - Do NOT add sentence-transformers/openai yet (Plan 03 handles that to keep Wave 1 lean)

    Step D - Create `app/database/vector_availability.py`:
      ```python
      """Vector search availability predicate and FastAPI dependency.

      Used by every vector-adjacent route to short-circuit cleanly on local SQLite
      (per D-08: vector endpoints return 503 when Turso is not configured).
      """
      from fastapi import HTTPException
      from .connection import get_database
      from ..config import get_settings


      def is_vector_enabled() -> bool:
          settings = get_settings()
          # Explicit override wins
          if settings.vector_search_enabled is False:
              return False
          db = get_database()
          has_turso = getattr(db, "_use_turso", False)
          if settings.vector_search_enabled is True:
              return has_turso  # Cannot force-enable without Turso
          # None = auto
          return has_turso


      def require_vector() -> None:
          """FastAPI dependency: raise RFC 7807 503 if vector search unavailable."""
          if not is_vector_enabled():
              raise HTTPException(
                  status_code=503,
                  detail={
                      "type": "https://openhub.dev/problems/vector-unavailable",
                      "title": "Vector search unavailable",
                      "status": 503,
                      "detail": (
                          "Vector search requires Turso configuration. "
                          "Set AGENTHUB_TURSO_DATABASE_URL and AGENTHUB_TURSO_AUTH_TOKEN."
                      ),
                  },
              )
      ```

    Step E - Write the three test files described in <behavior>. Use monkeypatch for env vars; use tempfile.NamedTemporaryFile for SQLite DB path. For migration tests, invoke Alembic via `from alembic.config import Config; from alembic import command; command.upgrade(cfg, "head")` with a dynamically set sqlalchemy.url.

    Per CLAUDE.md rules: no em dashes anywhere, snake_case function names, structlog not print, pytest fixtures for setup.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_vector_migration.py tests/unit/test_vector_deps.py tests/unit/test_vector_feature_flag.py -v --tb=short --no-cov -m "not turso"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "revision = '0003'" alembic/versions/0003_vector_columns.py` succeeds
    - `grep -q "F32_BLOB(384)" alembic/versions/0003_vector_columns.py` succeeds
    - `grep -q "libsql_vector_idx" alembic/versions/0003_vector_columns.py` succeeds
    - `grep -q "^zvec" requirements.txt` returns exit 1 (no match)
    - `grep -q "zvec_path" app/config.py` returns exit 1 (no match)
    - `grep -q "embedding_provider" app/config.py` succeeds
    - `grep -q "vector_search_enabled" app/config.py` succeeds
    - `grep -q "openai_api_key" app/config.py` succeeds
    - `grep -q "def is_vector_enabled" app/database/vector_availability.py` succeeds
    - `grep -q "def require_vector" app/database/vector_availability.py` succeeds
    - `grep -q "status_code=503" app/database/vector_availability.py` succeeds
    - All 3 new pytest files pass with `-m "not turso"`
    - `.venv/bin/python -c "from app.database.vector_availability import is_vector_enabled, require_vector; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
    Migration file exists and is idempotent on local SQLite, zvec is fully removed from requirements.txt and config.py, vector_availability module exists with is_vector_enabled/require_vector, and 3 test files pass without Turso credentials.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test scaffolds + pytest turso marker registration</name>
  <files>
    tests/conftest.py,
    pyproject.toml,
    tests/unit/test_embedding_service.py,
    tests/unit/test_auto_index.py,
    tests/unit/test_retry_worker.py,
    tests/integration/test_vector_storage.py,
    tests/integration/test_auto_indexing.py,
    tests/integration/test_vector_search.py,
    tests/integration/test_search_api.py
  </files>
  <read_first>
    - tests/conftest.py (current session fixtures and pytest setup)
    - pyproject.toml (current [tool.pytest.ini_options] markers list)
    - .planning/phases/03-vector-database/03-VALIDATION.md (full Wave 0 Requirements table)
    - .planning/phases/03-vector-database/03-RESEARCH.md lines 680-706 (Approach A mark-and-skip pattern)
  </read_first>
  <action>
    Step A - Edit `tests/conftest.py`:
      - Add `import os` and `import pytest` at top if absent
      - Add a `pytest_configure(config)` hook (or extend existing) that calls `config.addinivalue_line("markers", "turso: requires live Turso DB for vector tests")`
      - Add a `pytest_collection_modifyitems(config, items)` hook that auto-skips any item keyed `turso` unless BOTH `os.environ.get("TURSO_DATABASE_URL")` and `os.environ.get("TURSO_AUTH_TOKEN")` are set; skip reason: "Turso credentials not set"
      - Add a `mock_embedding_backend` fixture: returns an object with attrs `dim=384`, `model_name="mock"`, and an `async def embed(self, texts)` method returning `[[float(i)] * 384 for i,_ in enumerate(texts)]`. Yield a single instance per function scope.
      - Do NOT remove or change any existing fixtures

    Step B - Edit `pyproject.toml`:
      - Under `[tool.pytest.ini_options]`, add or append to `markers = [...]` the entry `"turso: requires live Turso DB for vector tests"`
      - If no markers list exists yet, create one

    Step C - Create the 7 remaining test stub files. Each file should:
      - Include a module docstring noting "Stub from Wave 0 - implementations filled in by later plans"
      - Import pytest
      - Contain at least one test function that passes (use `assert True` placeholder OR `@pytest.mark.xfail(reason="not yet implemented")` for tests that will be written later)
      - Turso-only test files (test_vector_storage.py, test_vector_search.py) add module-level `pytestmark = pytest.mark.turso`

    Specific stubs:
      - `tests/unit/test_embedding_service.py`: stubs for `test_local_dim`, `test_openai_dim_mocked`, `test_provider_selection_local`, `test_provider_selection_openai_missing_key`, `test_provider_selection_openai_present`. Mark all as `@pytest.mark.xfail(reason="Plan 03 implements")`.
      - `tests/unit/test_auto_index.py`: stubs for `test_background_task_scheduled`, `test_failed_status`. Mark xfail "Plan 04 implements".
      - `tests/unit/test_retry_worker.py`: stub `test_retry_picks_up_failed_rows` xfail "Plan 04 implements".
      - `tests/integration/test_vector_storage.py`: `pytestmark = pytest.mark.turso`; stubs `test_binding_roundtrip` xfail "Plan 02 implements (Turso smoke test)".
      - `tests/integration/test_auto_indexing.py`: stub `test_write_schedules_embedding` xfail "Plan 04 implements".
      - `tests/integration/test_vector_search.py`: `pytestmark = pytest.mark.turso`; stubs `test_roundtrip`, `test_ranking`, `test_index_created` xfail "Plan 02/05 implements".
      - `tests/integration/test_search_api.py`: stubs `test_503_local`, `test_top_k_cap`, `test_shortcut_delegation`, `test_flag_off`, `test_openapi_beta_tag`. Mix: `test_503_local` and `test_flag_off` should PASS now (test the Plan 01 require_vector behavior); others xfail "Plan 05/06 implements".

    Per CLAUDE.md rules: no em dashes, snake_case throughout, structlog not print.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/ -v --tb=short --no-cov -m "not turso" --co -q && .venv/bin/python -m pytest tests/unit/test_vector_migration.py tests/unit/test_vector_deps.py tests/unit/test_vector_feature_flag.py tests/unit/test_embedding_service.py tests/unit/test_auto_index.py tests/unit/test_retry_worker.py tests/integration/test_search_api.py tests/integration/test_auto_indexing.py -v --tb=short --no-cov -m "not turso"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "turso" pyproject.toml` succeeds (marker registered)
    - `grep -q "pytest.mark.turso" tests/conftest.py` OR `grep -q "turso:" tests/conftest.py` succeeds
    - `grep -q "mock_embedding_backend" tests/conftest.py` succeeds
    - All 7 new test stub files exist (ls returns success for each)
    - `grep -l "pytestmark = pytest.mark.turso" tests/integration/test_vector_storage.py tests/integration/test_vector_search.py` returns both files
    - pytest collection does not error under `-m "not turso"`
    - test_search_api.py::test_503_local and test_flag_off actually PASS (not xfail) because Plan 01 provides require_vector
    - Running `.venv/bin/python -m pytest -m turso --collect-only -q` in an environment without Turso creds shows all turso-marked tests as skipped, not errored
  </acceptance_criteria>
  <done>
    pytest `turso` marker registered; all Wave 0 stub files exist and collect cleanly; non-turso test suite stays green; turso-marked tests skip gracefully when credentials are absent.
  </done>
</task>

</tasks>

<verification>
Full phase verification at end of Plan 01:
- `.venv/bin/python -m pytest tests/ -x -q --tb=short --no-cov -m "not turso"` passes
- `.venv/bin/alembic upgrade head` runs against a fresh tempfile SQLite DB without raising
- `.venv/bin/python -c "from app.main import app; print('ok')"` still prints `ok` (no startup regression from config changes)
- `pip check` does not complain about zvec
</verification>

<success_criteria>
- Migration 0003 adds vector columns on all 4 target tables and is safely idempotent on local SQLite
- zvec is fully purged from requirements.txt and app/config.py
- New settings (embedding_provider, openai_api_key, vector_search_enabled) are addressable via AGENTHUB_ env vars
- vector_availability.is_vector_enabled() and require_vector() work and return RFC 7807 503 on local SQLite
- pytest turso marker skips cleanly when credentials absent
- All Wave 0 test stub files exist and collection succeeds
- Non-turso suite remains fully green
</success_criteria>

<output>
After completion, create `.planning/phases/03-vector-database/03-01-SUMMARY.md` documenting: migration revision number, exact columns added, settings added/removed, test files created, and any gotchas encountered during Alembic idempotency work.
</output>
