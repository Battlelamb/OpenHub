---
phase: 03-vector-database
plan: 06
type: execute
wave: 4
depends_on:
  - "03-04"
  - "03-05"
files_modified:
  - app/main.py
  - app/api/routes_search.py
  - README.md
  - CHANGELOG.md
  - .env.example
  - tests/unit/test_vector_feature_flag.py
  - tests/integration/test_search_api.py
autonomous: true
requirements:
  - VEC-06
user_setup: []
must_haves:
  truths:
    - "When AGENTHUB_VECTOR_SEARCH_ENABLED is explicitly set to false, /v1/search returns 503 even when Turso is configured"
    - "When Turso is configured and flag is unset, /v1/search is operational (auto-enable behavior)"
    - "App startup logs a WARNING when running on local SQLite without Turso - 'vector search disabled: Turso not configured'"
    - "OpenAPI schema tags include a description for search [experimental] noting opt-in beta status"
    - "README contains a 'Vector Search (Beta)' section explaining opt-in, env vars, and limitations"
    - "CHANGELOG entry for Phase 3 lists VEC-01..VEC-06 and marks the feature as beta/experimental"
    - ".env.example includes AGENTHUB_EMBEDDING_PROVIDER, AGENTHUB_OPENAI_API_KEY, AGENTHUB_VECTOR_SEARCH_ENABLED with comments"
  artifacts:
    - path: "README.md"
      provides: "Vector Search (Beta) section documenting opt-in, setup, and limitations"
      contains: "Vector Search"
    - path: "CHANGELOG.md"
      provides: "Phase 3 entry marking beta status"
      contains: "VEC-01"
    - path: ".env.example"
      provides: "Documented vector-related env vars"
      contains: "AGENTHUB_VECTOR_SEARCH_ENABLED"
    - path: "app/main.py"
      provides: "Startup warning log when vector disabled"
      contains: "vector_search_disabled"
  key_links:
    - from: "app/main.py lifespan startup"
      to: "app/database/vector_availability.is_vector_enabled"
      via: "conditional warning log"
      pattern: "vector_search_disabled"
    - from: "README.md Vector Search section"
      to: "AGENTHUB_EMBEDDING_PROVIDER + AGENTHUB_OPENAI_API_KEY + AGENTHUB_VECTOR_SEARCH_ENABLED env vars"
      via: "documentation"
      pattern: "AGENTHUB_"
---

<objective>
Close out VEC-06: feature-flagged as opt-in beta with documented experimental status. This plan adds the startup warning (D-09), ensures OpenAPI and docs clearly mark the feature as beta, extends the flag test coverage to include explicit-false override, and updates README/CHANGELOG/.env.example so OSS users can discover and enable the feature.

Purpose: VEC-06 requires the feature to be opt-in and documented. Plans 01-05 made it functionally work and flag-gated; this plan makes it user-visible as beta without hiding it.
Output: Documentation updates, startup warning wiring, OpenAPI tag description, expanded flag tests, and a full non-turso test suite pass confirming the phase goal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-vector-database/03-CONTEXT.md
@.planning/phases/03-vector-database/03-RESEARCH.md
@.planning/phases/03-vector-database/03-VALIDATION.md
@.planning/phases/03-vector-database/03-04-SUMMARY.md
@.planning/phases/03-vector-database/03-05-SUMMARY.md

@app/main.py
@app/api/routes_search.py
@app/database/vector_availability.py
@README.md
@CHANGELOG.md

<interfaces>
From Plan 01 - app/config.py Settings:
```python
vector_search_enabled: Optional[bool] = None  # None = auto-detect, True/False = override
embedding_provider: str = "local"
openai_api_key: Optional[str] = None
```

From Plan 01 - app/database/vector_availability.py:
```python
def is_vector_enabled() -> bool:
    # Returns False if settings.vector_search_enabled is False
    # Returns db._use_turso when settings.vector_search_enabled is None (auto)
    # Cannot force-enable without Turso even when flag is True
```

Expected OpenAPI change:
- Router tag "search [experimental]" gets a tag description added at app-level:
  "Semantic vector search over memories, tasks, artifacts, and messages. BETA: requires Turso configuration; see README."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Startup warning + OpenAPI tag description + flag test hardening</name>
  <files>app/main.py, app/api/routes_search.py, tests/unit/test_vector_feature_flag.py, tests/integration/test_search_api.py</files>
  <read_first>
    - app/main.py (current lifespan and FastAPI() constructor - look for openapi_tags parameter if any)
    - app/database/vector_availability.py (is_vector_enabled logic)
    - app/config.py (Settings after Plan 01)
    - tests/unit/test_vector_feature_flag.py (tests from Plan 01 Task 1 - extend these)
    - tests/integration/test_search_api.py (test_flag_off stub from Plan 01 / real tests from Plan 05)
  </read_first>
  <behavior>
    - test_startup_logs_warning_on_local_sqlite: patch is_vector_enabled -> False, trigger lifespan startup, capture structlog output, assert a warning event named "vector_search_disabled" (or similar) was logged
    - test_startup_no_warning_when_enabled: is_vector_enabled -> True, no such warning emitted
    - test_explicit_false_overrides_turso: monkeypatch _use_turso True AND vector_search_enabled=False, assert is_vector_enabled() returns False
    - test_none_auto_detects_turso: vector_search_enabled=None AND _use_turso=True, returns True
    - test_none_auto_detects_no_turso: vector_search_enabled=None AND _use_turso=False, returns False
    - test_true_without_turso_still_false: vector_search_enabled=True but _use_turso=False, returns False (cannot force-enable per Plan 01 semantics)
    - test_openapi_tag_has_description: GET /openapi.json, find the "search [experimental]" tag in the top-level tags array, assert it has a description containing "BETA" or "experimental" or "opt-in"
    - test_search_endpoint_marked_experimental_in_operation: /v1/search POST operation's tags list contains "search [experimental]"
  </behavior>
  <action>
    Step A - Edit `app/main.py`:
      - In `FastAPI(...)` constructor (or wherever `app = FastAPI(...)` is), add an `openapi_tags` parameter (or extend existing) with an entry:
        ```python
        {
          "name": "search [experimental]",
          "description": (
              "Semantic vector search over memories, tasks, artifacts, and messages. "
              "BETA: opt-in feature that requires Turso configuration. "
              "Set AGENTHUB_VECTOR_SEARCH_ENABLED=true and provide TURSO_DATABASE_URL + TURSO_AUTH_TOKEN. "
              "See README Vector Search section for full setup."
          ),
        }
        ```
      - In the lifespan startup block (after existing service starts, before yield), add:
        ```python
        from .database.vector_availability import is_vector_enabled
        if not is_vector_enabled():
            logger.warning(
                "vector_search_disabled",
                reason="Turso not configured or feature flag off",
                hint="Set AGENTHUB_TURSO_DATABASE_URL + AGENTHUB_TURSO_AUTH_TOKEN and AGENTHUB_VECTOR_SEARCH_ENABLED=true",
            )
        else:
            logger.info("vector_search_enabled")
        ```
      - Ensure the existing `logger` in main.py is structlog (if not, use `structlog.get_logger(__name__)`)

    Step B - Edit `app/api/routes_search.py`:
      - No behavioral change. Add a module-level docstring stating "BETA: opt-in vector search. See README." if not already present.
      - Confirm the router still uses `tags=["search [experimental]"]` so it matches the tag description in main.py

    Step C - Extend `tests/unit/test_vector_feature_flag.py` (append to the Plan 01 tests):
      - `test_explicit_false_overrides_turso`: monkeypatch Database instance to set `_use_turso = True`, set settings vector_search_enabled=False, assert is_vector_enabled() is False
      - `test_true_without_turso_still_false`: Database `_use_turso=False`, settings vector_search_enabled=True, assert is_vector_enabled() is False
      - `test_none_auto_detects_turso`: _use_turso=True, vector_search_enabled=None, assert True
      - `test_none_auto_detects_no_turso`: _use_turso=False, vector_search_enabled=None, assert False
      - Use monkeypatch.setattr on get_database and get_settings cached instances

    Step D - Extend `tests/integration/test_search_api.py`:
      - `test_openapi_tag_has_description`: GET `/openapi.json`, find tags array at top level, assert presence of entry with name containing "experimental" and description containing "BETA"
      - `test_search_endpoint_marked_experimental`: GET `/openapi.json`, assert the `/v1/search` POST operation has a tag containing "experimental"
      - `test_startup_warning_logged_on_local_sqlite`: use `caplog` or structlog's testing helpers to capture log records during TestClient startup, assert a record with message_key "vector_search_disabled" exists when running on local SQLite

    Per CLAUDE.md: no em dashes, structured logging, snake_case.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_vector_feature_flag.py tests/integration/test_search_api.py -v --tb=short --no-cov -m "not turso"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "vector_search_disabled" app/main.py` succeeds (startup warning wired)
    - `grep -q "openapi_tags" app/main.py` succeeds OR `grep -q 'name.*search \[experimental\]' app/main.py` succeeds
    - `grep -q "BETA" app/main.py` succeeds (tag description content)
    - All tests in tests/unit/test_vector_feature_flag.py pass (original 3 + 4 new)
    - All flag + openapi tests in tests/integration/test_search_api.py pass
    - `.venv/bin/python -c "from fastapi.testclient import TestClient; from app.main import app; c=TestClient(app); data=c.get('/openapi.json').json(); tags=data.get('tags',[]); exp=[t for t in tags if 'experimental' in t.get('name','')]; assert exp and 'BETA' in exp[0].get('description',''); print('ok')"` prints `ok`
    - Non-turso full suite still green: `.venv/bin/python -m pytest tests/ -x -q -m "not turso"`
  </acceptance_criteria>
  <done>
    Startup logs a clear warning on local SQLite, OpenAPI advertises the feature as BETA, flag semantics are fully covered by unit tests, and the opt-in beta contract is enforceable by automated tests.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: README, CHANGELOG, and .env.example updates</name>
  <files>README.md, CHANGELOG.md, .env.example</files>
  <read_first>
    - README.md (current structure - find a good place to insert a new section, probably after Quickstart or Configuration)
    - CHANGELOG.md (current format - mimic existing Phase 1/2 entries)
    - .env.example (current env var list - add near existing AGENTHUB_ vars)
    - .planning/phases/03-vector-database/03-CONTEXT.md (decisions D-01..D-25 for accurate documentation)
  </read_first>
  <action>
    Step A - Edit `README.md`, add a new section titled `## Vector Search (Beta)`:
      Content outline (use regular dashes, no em dashes):
      - Opening line: "OpenHub ships with optional semantic search over memories, tasks, artifacts, and messages. This feature is in beta and requires Turso."
      - Subsection `### Requirements`:
        * Turso database (free tier at https://turso.tech) with TURSO_DATABASE_URL and TURSO_AUTH_TOKEN set
        * Either sentence-transformers installed locally (default, ~350MB of ML deps) OR an OpenAI API key
      - Subsection `### Setup`:
        * Export env vars (example shell block):
          ```
          export AGENTHUB_TURSO_DATABASE_URL="libsql://your-db.turso.io"
          export AGENTHUB_TURSO_AUTH_TOKEN="your-token"
          export AGENTHUB_EMBEDDING_PROVIDER="local"  # or "openai"
          export AGENTHUB_OPENAI_API_KEY=""           # required if provider=openai
          export AGENTHUB_VECTOR_SEARCH_ENABLED="true"  # or leave unset for auto
          ```
        * Run `alembic upgrade head` to add vector columns and DiskANN index
        * Verify with `curl -X POST http://localhost:7788/v1/search -H 'Content-Type: application/json' -d '{"query":"hello","top_k":5}'`
      - Subsection `### API`:
        * `POST /v1/search` unified endpoint
        * `POST /v1/memory/search`, `/v1/tasks/search`, `/v1/artifacts/search`, `/v1/messaging/search` shortcuts
        * Request body: `{query, types?, filters?, top_k}` (top_k 1..50, default 10)
        * Response: list of `SearchHit` with entity_type, id, content, distance
      - Subsection `### Limitations (v1 Beta)`:
        * Requires Turso; local SQLite returns 503 on vector endpoints
        * English only
        * No cross-encoder re-ranking
        * Text truncated to 30000 chars before embedding
        * Provider switch requires schema migration (dimensions differ)
      - Subsection `### Disabling`:
        * Set `AGENTHUB_VECTOR_SEARCH_ENABLED=false` to explicitly disable even when Turso is configured

    Step B - Edit `CHANGELOG.md`, add a new entry at the top (or in the Unreleased section):
      ```
      ## [Phase 3 - Vector Database] - 2026-04-XX

      ### Added (BETA / Experimental)
      - VEC-01: Turso/libSQL native vector columns (F32_BLOB) replacing zvec
      - VEC-02: Semantic search via vector_distance_cos
      - VEC-03: DiskANN indexing via libsql_vector_idx
      - VEC-04: Auto-indexing hooks on memory/task/artifact/message write paths (async via BackgroundTasks)
      - VEC-05: POST /v1/search unified endpoint + per-entity shortcuts
      - VEC-06: Opt-in beta with AGENTHUB_VECTOR_SEARCH_ENABLED flag
      - New settings: AGENTHUB_EMBEDDING_PROVIDER (local|openai), AGENTHUB_OPENAI_API_KEY, AGENTHUB_VECTOR_SEARCH_ENABLED
      - New endpoints tagged [experimental] in OpenAPI /docs

      ### Removed
      - zvec dependency (replaced by native Turso vector columns)
      - AGENTHUB_ZVEC_PATH config setting

      ### Notes
      - Vector search requires Turso configuration; local SQLite returns 503 on vector endpoints
      - sentence-transformers and openai are now in requirements.txt (local backend is default)
      - See README "Vector Search (Beta)" section for setup instructions
      ```

    Step C - Edit `.env.example` (or create if absent), ADD (do not duplicate existing entries):
      ```
      # Vector Search (Phase 3 Beta) - optional, requires Turso
      # AGENTHUB_VECTOR_SEARCH_ENABLED=true    # or leave unset for auto-detect from Turso
      # AGENTHUB_EMBEDDING_PROVIDER=local      # "local" (sentence-transformers) or "openai"
      # AGENTHUB_OPENAI_API_KEY=               # required only when EMBEDDING_PROVIDER=openai
      ```
      Place these near existing TURSO_* env vars if present, otherwise at the bottom under a clear header comment.

    Per CLAUDE.md: no em dashes anywhere in docs, hyphens/colons/commas only.
  </action>
  <verify>
    <automated>grep -q "Vector Search" README.md && grep -q "VEC-01" CHANGELOG.md && grep -q "AGENTHUB_VECTOR_SEARCH_ENABLED" .env.example && ! grep -- " — " README.md CHANGELOG.md .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Vector Search (Beta)" README.md` succeeds
    - `grep -q "AGENTHUB_EMBEDDING_PROVIDER" README.md` succeeds
    - `grep -q "AGENTHUB_VECTOR_SEARCH_ENABLED" README.md` succeeds
    - `grep -q "/v1/search" README.md` succeeds
    - `grep -q "top_k" README.md` succeeds
    - `grep -q "BETA\|beta\|experimental" CHANGELOG.md` succeeds
    - `grep -q "VEC-01" CHANGELOG.md` succeeds
    - `grep -q "VEC-06" CHANGELOG.md` succeeds
    - `grep -q "AGENTHUB_VECTOR_SEARCH_ENABLED" .env.example` succeeds
    - `grep -q "AGENTHUB_EMBEDDING_PROVIDER" .env.example` succeeds
    - `grep -q "AGENTHUB_OPENAI_API_KEY" .env.example` succeeds
    - No em dashes in any edited doc: `grep -c -- " — " README.md CHANGELOG.md .env.example` reports 0 across all files
    - Markdown still renders: `.venv/bin/python -c "open('README.md').read(); open('CHANGELOG.md').read(); print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
    README has a clear Vector Search (Beta) section with setup, API, limitations, and disabling instructions. CHANGELOG lists all 6 VEC requirements and the zvec removal. .env.example documents all 3 new env vars. No em dashes anywhere.
  </done>
</task>

</tasks>

<verification>
- Full non-turso test suite: `.venv/bin/python -m pytest tests/ -x -q --tb=short --no-cov -m "not turso"` passes
- Full turso-gated test suite (with credentials): `.venv/bin/python -m pytest tests/ -x -q -m turso` passes (or skips cleanly)
- Smoke app boot: `.venv/bin/python -c "from app.main import app; print('ok')"`
- Phase goal check: curl to /v1/search on local SQLite returns 503 with RFC 7807 body; curl with Turso + seeded data returns SearchHits
- `grep -c -- " — " README.md CHANGELOG.md .env.example app/` should be 0 (no em dashes anywhere in phase artifacts)
</verification>

<success_criteria>
- VEC-06 is satisfied: feature is flag-gated, opt-in, documented as beta in OpenAPI + README + CHANGELOG
- Startup logs a clear warning when vector search is disabled (so developers notice)
- Flag test coverage includes all 4 combinations of (Turso, flag) states
- README explains setup for both local and OpenAI backends
- CHANGELOG lists all 6 requirements delivered in Phase 3
- .env.example lists all 3 new env vars with comments
- Full non-turso test suite still green
- Phase 3 can be shipped as opt-in beta without surprising any existing user
</success_criteria>

<output>
After completion, create `.planning/phases/03-vector-database/03-06-SUMMARY.md` documenting: final OpenAPI tag description wording, exact CHANGELOG entry, README section location, and confirmation that the full test suite (both with and without Turso) is green. Also note any TODO items left for Phase 5 (OSS release) like extras_require for vector dependencies.
</output>
