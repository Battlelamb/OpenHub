---
phase: 03-vector-database
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/services/embedding_service.py
  - requirements.txt
  - pyproject.toml
  - tests/unit/test_embedding_service.py
autonomous: true
requirements:
  - VEC-04
user_setup: []
must_haves:
  truths:
    - "LocalSentenceTransformerBackend.embed returns 384-dim vectors for a list of text inputs"
    - "OpenAIBackend.embed returns 1536-dim vectors when mocked AsyncOpenAI client is injected"
    - "get_embedding_service() returns LocalSentenceTransformerBackend when AGENTHUB_EMBEDDING_PROVIDER=local (or unset)"
    - "get_embedding_service() returns OpenAIBackend when AGENTHUB_EMBEDDING_PROVIDER=openai and AGENTHUB_OPENAI_API_KEY is set"
    - "get_embedding_service() returns None when provider=openai but key missing (graceful degradation per D-03)"
    - "sentence-transformers is NOT imported at module import time (lazy load avoids ~200MB torch cold start)"
  artifacts:
    - path: "app/services/embedding_service.py"
      provides: "EmbeddingBackend Protocol, LocalSentenceTransformerBackend, OpenAIBackend, get_embedding_service() factory"
      exports: ["EmbeddingBackend", "LocalSentenceTransformerBackend", "OpenAIBackend", "get_embedding_service"]
    - path: "requirements.txt"
      provides: "sentence-transformers and openai additions for vector backends"
      contains: "sentence-transformers"
  key_links:
    - from: "app/services/embedding_service.py get_embedding_service()"
      to: "app/config.py Settings.embedding_provider + Settings.openai_api_key"
      via: "get_settings() lookup"
      pattern: "embedding_provider"
    - from: "LocalSentenceTransformerBackend.embed"
      to: "sentence_transformers.SentenceTransformer"
      via: "lazy import inside _ensure_model()"
      pattern: "from sentence_transformers import"
---

<objective>
Implement the pluggable EmbeddingService with local (sentence-transformers) and OpenAI backends, lazy-loaded to avoid torch cold-start cost at app boot. This is the sibling of Plan 02 in Wave 2: independent files, no conflicts, runs in parallel.

Purpose: Plans 04 (auto-indexing) and 05 (search) both depend on get_embedding_service(). Getting backend selection, lazy loading, and graceful degradation right here keeps the hot paths in later plans simple.
Output: embedding_service.py module with Protocol + 2 concrete backends + factory, updated requirements.txt with torch-free install path as the default, and a unit test file covering backend selection and dimension contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-vector-database/03-CONTEXT.md
@.planning/phases/03-vector-database/03-RESEARCH.md
@.planning/phases/03-vector-database/03-VALIDATION.md
@.planning/phases/03-vector-database/03-01-SUMMARY.md

@app/config.py
@app/logging.py
@requirements.txt

<interfaces>
From app/config.py (updated in Plan 01):
```python
class Settings(BaseSettings):
    embedding_provider: str = "local"      # "local" | "openai"
    openai_api_key: Optional[str] = None
    embedding_model: str = "sentence-transformers/all-MiniLM-L6-v2"  # existing
def get_settings() -> Settings: ...
```

Target Protocol (from RESEARCH.md Pattern 1):
```python
class EmbeddingBackend(Protocol):
    dim: int
    model_name: str
    async def embed(self, texts: List[str]) -> List[List[float]]: ...
```

Model contracts (from D-01, D-05):
- Local:  dim=384,  model_name="sentence-transformers/all-MiniLM-L6-v2"
- OpenAI: dim=1536, model_name="text-embedding-3-small"

Anti-patterns (from RESEARCH.md Pitfalls 3, 4):
- NEVER `from sentence_transformers import SentenceTransformer` at module top
- NEVER call SentenceTransformer(...) at lifespan startup
- Always lazy-load inside _ensure_model() with asyncio.Lock double-check
- Run blocking model load and encode() in run_in_executor (threadpool)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EmbeddingService module with lazy-loaded local and OpenAI backends</name>
  <files>app/services/embedding_service.py, tests/unit/test_embedding_service.py</files>
  <read_first>
    - app/config.py (Settings after Plan 01, confirm embedding_provider and openai_api_key fields exist)
    - app/logging.py (get_logger pattern)
    - .planning/phases/03-vector-database/03-RESEARCH.md lines 149-230 (Pattern 1 reference)
    - .planning/phases/03-vector-database/03-RESEARCH.md lines 538-542 (Pitfall 4: fork safety)
    - tests/conftest.py (mock_embedding_backend fixture from Plan 01 Task 2)
    - tests/unit/test_embedding_service.py (xfail stubs from Plan 01 Task 2 - to be replaced)
  </read_first>
  <behavior>
    - test_local_dim: LocalSentenceTransformerBackend.dim == 384 and model_name == "sentence-transformers/all-MiniLM-L6-v2" (class attribute access, no model load)
    - test_openai_dim_mocked: OpenAIBackend.dim == 1536 and model_name == "text-embedding-3-small"
    - test_local_embed_returns_correct_shape: patching SentenceTransformer with a fake returning np-array-like with 384 dims, `await backend.embed(["hello", "world"])` returns a list of 2 lists of 384 floats
    - test_local_lazy_load: after construction, `backend._model is None`; after first embed call, `backend._model is not None`
    - test_local_no_module_import_on_class_construction: `LocalSentenceTransformerBackend()` does NOT cause `sentence_transformers` to appear in sys.modules (use `sys.modules` snapshot before/after)
    - test_openai_embed_calls_client: patch AsyncOpenAI, call backend.embed, assert embeddings.create was called with model="text-embedding-3-small"
    - test_openai_missing_key_returns_none: monkeypatch AGENTHUB_EMBEDDING_PROVIDER=openai and clear openai_api_key, assert get_embedding_service() returns None
    - test_openai_with_key_returns_openai_backend: monkeypatch provider=openai and key set, assert isinstance returned == OpenAIBackend
    - test_local_is_default: no env vars set, isinstance(get_embedding_service(), LocalSentenceTransformerBackend)
    - test_provider_unknown_falls_back_to_local: AGENTHUB_EMBEDDING_PROVIDER=bogus returns LocalSentenceTransformerBackend (soft fallback)
  </behavior>
  <action>
    Step A - Create `app/services/embedding_service.py`:
      - Module docstring explaining lazy-load rationale and fork-safety (cite Pitfall 4)
      - Imports at top: `import asyncio`, `from typing import List, Optional, Protocol, runtime_checkable`, `from ..config import get_settings`, `from ..logging import get_logger`
      - IMPORTANT: do NOT import sentence_transformers or openai at module top
      - `logger = get_logger(__name__)`
      - `@runtime_checkable class EmbeddingBackend(Protocol)` with `dim: int`, `model_name: str`, `async def embed(self, texts: List[str]) -> List[List[float]]`
      - `class LocalSentenceTransformerBackend`:
        * Class attrs: `dim: int = 384`, `model_name: str = "sentence-transformers/all-MiniLM-L6-v2"`
        * `__init__`: `self._model = None`, `self._lock = asyncio.Lock()`
        * `async def _ensure_model(self)`: double-checked locking, import `from sentence_transformers import SentenceTransformer` INSIDE the if-block, run `SentenceTransformer(self.model_name)` via `loop.run_in_executor(None, lambda: ...)`, log `embedding_model_loaded`
        * `async def embed(self, texts: List[str]) -> List[List[float]]`: call `_ensure_model()`, then run `self._model.encode(texts, convert_to_numpy=True).tolist()` in run_in_executor
      - `class OpenAIBackend`:
        * Class attrs: `dim: int = 1536`, `model_name: str = "text-embedding-3-small"`
        * `__init__(self, api_key: str, client=None)`: accept optional `client` param for test injection; if None, import AsyncOpenAI INSIDE init and construct with `max_retries=3`
        * `async def embed(self, texts: List[str]) -> List[List[float]]`: `resp = await self._client.embeddings.create(model=self.model_name, input=texts)`; return `[item.embedding for item in resp.data]`; truncate each input to 30000 chars first to avoid >8192 token errors (Pitfall 7)
      - `def get_embedding_service() -> Optional[EmbeddingBackend]`:
        * `settings = get_settings()`
        * `provider = settings.embedding_provider`
        * If provider == "openai": check settings.openai_api_key; if missing, log warning `embedding_provider_openai_missing_key_disabled` and return None; else return `OpenAIBackend(settings.openai_api_key)`
        * Default/else branch (including unknown provider): return `LocalSentenceTransformerBackend()` and log `embedding_provider_local` at debug

    Step B - Update `requirements.txt`:
      - Append (keep existing lines intact, alphabetical where possible):
        * `sentence-transformers==3.3.1` (or latest stable verified via `pip index versions` - if 3.3.1 unavailable, pin to latest 3.x)
        * `openai==1.54.0` (or latest 1.x verified)
      - Add a comment line above these: `# Vector search backends (Phase 3)`

    Step C - Update `pyproject.toml` to add an optional dependency group (per RESEARCH Open Question 4):
      - Under `[tool.poetry.extras]` (or equivalent) add `vector = ["sentence-transformers", "openai", "torch"]` if Poetry is configured; otherwise add a `# optional: see requirements-vector.txt` comment and skip.
      - If neither works cleanly, document in the summary that extras will be introduced in Phase 5 (OSS release).

    Step D - Replace `tests/unit/test_embedding_service.py` stubs with real tests per <behavior>:
      - Remove `@pytest.mark.xfail` markers
      - Use `monkeypatch.setenv("AGENTHUB_EMBEDDING_PROVIDER", ...)` + `monkeypatch.setattr` on get_settings cache as needed to reset
      - Use `unittest.mock.AsyncMock` for AsyncOpenAI client injection
      - For sentence_transformers-dependent tests, use `monkeypatch.setitem(sys.modules, "sentence_transformers", fake_module)` so torch is never imported during unit tests
      - `sys.modules` snapshot test: `snapshot = set(sys.modules); LocalSentenceTransformerBackend(); assert "sentence_transformers" not in (set(sys.modules) - snapshot)`
      - Use pytest-asyncio `@pytest.mark.asyncio` for async tests

    Per CLAUDE.md: no em dashes, snake_case, structlog not print, type hints on everything.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/unit/test_embedding_service.py -v --tb=short --no-cov</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class EmbeddingBackend" app/services/embedding_service.py` succeeds
    - `grep -q "class LocalSentenceTransformerBackend" app/services/embedding_service.py` succeeds
    - `grep -q "class OpenAIBackend" app/services/embedding_service.py` succeeds
    - `grep -q "def get_embedding_service" app/services/embedding_service.py` succeeds
    - `grep -q "dim: int = 384" app/services/embedding_service.py` succeeds
    - `grep -q "dim: int = 1536" app/services/embedding_service.py` succeeds
    - `grep -q "text-embedding-3-small" app/services/embedding_service.py` succeeds
    - `grep -q "all-MiniLM-L6-v2" app/services/embedding_service.py` succeeds
    - `grep -q "asyncio.Lock" app/services/embedding_service.py` succeeds (lazy-load lock present)
    - `grep -q "run_in_executor" app/services/embedding_service.py` succeeds (threadpool wrap present)
    - Module-level import check: `head -30 app/services/embedding_service.py | grep -E "^(from sentence_transformers|from openai)" | wc -l` equals 0 (no top-level ML imports)
    - `grep -q "sentence-transformers" requirements.txt` succeeds
    - `grep -q "^openai" requirements.txt` succeeds
    - All tests in tests/unit/test_embedding_service.py pass (no xfail remaining)
    - `.venv/bin/python -c "import sys; from app.services import embedding_service; assert 'sentence_transformers' not in sys.modules; assert 'torch' not in sys.modules; print('lazy ok')"` prints `lazy ok`
  </acceptance_criteria>
  <done>
    embedding_service.py has Protocol + 2 backends + factory, no ML libs imported at module top, unit tests prove lazy loading and backend selection rules. Plans 04 and 05 can call get_embedding_service() with confidence.
  </done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/unit/test_embedding_service.py -v` passes
- `.venv/bin/python -m pytest tests/ -x -q --tb=short --no-cov -m "not turso"` still passes end-to-end
- `.venv/bin/python -c "from app.main import app; import sys; print('torch' in sys.modules, 'sentence_transformers' in sys.modules)"` prints `False False` (app boots without touching ML deps)
- `pip install -r requirements.txt` succeeds in a clean venv (or documented as post-phase ops step if torch install is too heavy for CI)
</verification>

<success_criteria>
- EmbeddingBackend Protocol + 2 concrete backends + factory exist in one file
- Local backend lazily loads sentence-transformers only on first embed() call
- OpenAI backend uses AsyncOpenAI with max_retries=3
- Provider selection honors AGENTHUB_EMBEDDING_PROVIDER and gracefully degrades when openai key missing
- requirements.txt lists sentence-transformers and openai
- Unit tests cover dim contracts, backend selection, lazy load, and anti-fork behavior
- Non-turso test suite stays green
</success_criteria>

<output>
After completion, create `.planning/phases/03-vector-database/03-03-SUMMARY.md` documenting: exact pinned versions of sentence-transformers and openai, whether the optional dependency group was added to pyproject.toml or deferred, and any lazy-load test techniques (sys.modules snapshot patterns) that downstream plans should reuse.
</output>
