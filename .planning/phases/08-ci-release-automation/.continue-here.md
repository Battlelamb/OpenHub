---
context: phase
phase: 08-ci-release-automation
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-05-28T20:25:44Z
---

# Continue Here — Phase 08 CI + Release Automation

## Current State

Phase 08 is open. **08-01 — GitHub Actions CI workflow** and **08-02 — CI result follow-up** are complete. **08-03 — Docker dashboard packaging** is implemented locally and awaiting the GitHub Actions container smoke proof after push.

## 08-01 Completed

Created `.github/workflows/ci.yml` with four jobs:

- backend tests
- frontend audit/lint/typecheck/tests/build
- Compose + package smoke
- Playwright dashboard E2E against an ephemeral local API

Local verification completed:

- `git diff --check` passed.
- Workflow shape parse passed; PyYAML saw 4 jobs.
- `.planning/HANDOFF.json` parsed.
- GSD health healthy.
- GSD consistency passed with 6 known non-blocking warnings.
- Changed-file secret scan found no hits.
- Focused backend smoke: 8 passed.
- Frontend typecheck passed.

## 08-02 Completed

First CI run after `be500d6` failed before jobs were created because `${{ runner.temp }}` was used in job-level `env`. `actionlint` caught the invalid context. The workflow now uses `/tmp/openhub-e2e.db` for the Playwright E2E SQLite DB.

Rerun `26597813831` on commit `27f1703` passed:

- Backend tests — success (`1m39s`)
- Frontend lint, tests, and build — success (`45s`)
- Compose and package smoke — success (`32s`)
- Playwright dashboard E2E — success (`2m29s`)

Evidence: `.planning/phases/08-ci-release-automation/08-02-CI-RUN-EVIDENCE.md`

Advisory: GitHub emitted Node.js 20 action deprecation notices for current `actions/*@v4/v5`; not blocking.

## Active Slice — 08-03

Implemented locally:

1. Dockerfile now builds the dashboard in a `node:22-slim AS dashboard-build` stage.
2. Final Python image copies `/dashboard/dist` to `/app/web/dist`, which is the path mounted by `app.main` for `/dashboard`.
3. `.dockerignore` is now tracked and preserves frontend source/config/package inputs while excluding generated artifacts and dependency caches.
4. CI Compose/package smoke now builds the image, runs it, verifies `/v1/health/simple`, verifies `/dashboard`, extracts a `/dashboard/assets/<hash>.js|css` asset from the HTML, and verifies the asset response headers.
5. README and operations docs now document bundled Docker dashboard verification.

Local verification passed:

- `npm run build`
- `.venv/bin/python -m pytest tests/unit/test_docker_dashboard_packaging.py -q --tb=short` — 2 passed
- `.venv/bin/python -m pytest tests/unit/test_static_mount.py tests/unit/test_docker_dashboard_packaging.py -q --tb=short` — 8 passed
- `/tmp/openhub-tools/actionlint .github/workflows/ci.yml`
- workflow YAML parse — 4 jobs; Compose/package smoke has 7 steps
- `git diff --check`
- GSD health — healthy
- GSD consistency — passed with 6 known non-blocking warnings

Local Docker socket is not usable in this session (`docker info` permission denied; `sudo -n docker info` requires a password), so final runtime proof must come from GitHub Actions after push.

## Next Action

Commit and push 08-03, then watch the GitHub Actions run. If the new Docker dashboard smoke passes, update `08-03-DOCKER-DASHBOARD-PACKAGING.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/HANDOFF.json` to mark 08-03 complete and open **08-04 — Release/tag automation guardrail**.
