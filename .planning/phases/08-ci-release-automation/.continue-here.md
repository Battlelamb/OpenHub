---
context: phase
phase: 08-ci-release-automation
task: 4
total_tasks: 5
status: in_progress
last_updated: 2026-05-28T20:31:00Z
---

# Continue Here — Phase 08 CI + Release Automation

## Current State

Phase 08 is open. **08-01 — GitHub Actions CI workflow**, **08-02 — CI result follow-up**, and **08-03 — Docker dashboard packaging** are complete. The next slice is **08-04 — Release/tag automation guardrail**.

## Completed Slices

### 08-01 — GitHub Actions CI workflow

Created `.github/workflows/ci.yml` with four jobs:

- backend tests
- frontend audit/lint/typecheck/tests/build
- Compose + package smoke
- Playwright dashboard E2E against an ephemeral local API

Evidence: `.planning/phases/08-ci-release-automation/08-01-CI-WORKFLOW.md`

### 08-02 — CI result follow-up

First CI run after `be500d6` failed before jobs were created because `${{ runner.temp }}` was used in job-level `env`. `actionlint` caught the invalid context. The workflow now uses `/tmp/openhub-e2e.db` for the Playwright E2E SQLite DB.

Rerun `26597813831` on commit `27f1703` passed all four jobs. Later doc closeout run `26598053442` also passed.

Evidence: `.planning/phases/08-ci-release-automation/08-02-CI-RUN-EVIDENCE.md`

### 08-03 — Docker dashboard packaging

Docker packaging is now explicit and proven:

1. Dockerfile builds the dashboard in a `node:22-slim AS dashboard-build` stage.
2. Final Python image copies `/dashboard/dist` to `/app/web/dist`, which is the path mounted by `app.main` for `/dashboard`.
3. `.dockerignore` is tracked and preserves frontend source/config/package inputs while excluding generated artifacts and dependency caches.
4. CI Compose/package smoke builds the image, runs it, verifies `/v1/health/simple`, verifies `/dashboard`, extracts a `/dashboard/assets/<hash>.js|css` asset from the HTML, and verifies the asset response headers.
5. README and operations docs document bundled Docker dashboard verification.

Local verification passed:

- `npm run build`
- `.venv/bin/python -m pytest tests/unit/test_docker_dashboard_packaging.py -q --tb=short` — 2 passed
- `.venv/bin/python -m pytest tests/unit/test_static_mount.py tests/unit/test_docker_dashboard_packaging.py -q --tb=short` — 8 passed
- `/tmp/openhub-tools/actionlint .github/workflows/ci.yml`
- workflow YAML parse and `bash -n` for workflow run steps
- `git diff --check`
- changed/staged secret scan — no hits
- GSD health — healthy
- GSD consistency — passed with 6 known non-blocking warnings

CI verification:

- Run `26600306093` on commit `2ca0fb0` passed: https://github.com/Battlelamb/OpenHub/actions/runs/26600306093
- Compose/package smoke passed in `2m25s`, including `Build Docker image with bundled dashboard` and `Smoke Docker dashboard packaging`.
- Backend tests, frontend tests/build, and Playwright E2E also passed.

Evidence: `.planning/phases/08-ci-release-automation/08-03-DOCKER-DASHBOARD-PACKAGING.md`

Advisory: GitHub still emits non-blocking Node.js 20 action deprecation notices for current `actions/*` versions.

## Active Next Slice — 08-04

Design a safe release/tag automation guardrail. Keep it manual and non-publishing unless explicit secrets/version decisions are made.

Start from these questions:

1. Should OpenHub add a manual `workflow_dispatch` release verification workflow, docs-only release checklist, or both?
2. Which version target is appropriate when the operator chooses to release later: patch (`v0.1.1`) or minor (`v0.2.0`)? Do not create a tag without explicit instruction.
3. How should artifacts be prepared safely without PyPI/Docker registry publishing secrets by default?

## Fix Rules

- No automatic tag creation.
- No PyPI, Docker Hub, GHCR, or other publishing unless explicit secrets and version are selected.
- Keep release automation dry-run/manual by default.
- Keep Node.js 20 action deprecation cleanup as an advisory unless the slice chooses to opt into Node 24 safely.
