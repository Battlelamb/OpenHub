---
context: phase
phase: 08-ci-release-automation
task: 5
total_tasks: 5
status: remote_ci_pending
last_updated: 2026-05-28T20:47:30Z
---

# Continue Here — Phase 08 CI + Release Automation

## Current State

Phase 08 local implementation is complete. **08-01**, **08-02**, **08-03**, **08-04**, and **08-05** have local evidence. The active next step is to push the 08-04/08-05 commit and verify GitHub Actions.

## Completed Slices

### 08-01 — GitHub Actions CI workflow

Created `.github/workflows/ci.yml` with backend, frontend, Compose/package, and Playwright E2E jobs.

Evidence: `.planning/phases/08-ci-release-automation/08-01-CI-WORKFLOW.md`

### 08-02 — CI result follow-up

Fixed invalid job-level `runner.temp` usage. CI run `26597813831` passed all four jobs; later doc closeout run `26598053442` also passed.

Evidence: `.planning/phases/08-ci-release-automation/08-02-CI-RUN-EVIDENCE.md`

### 08-03 — Docker dashboard packaging

Docker image now builds `web/dist` in a Node stage, copies it into `/app/web/dist`, and CI run `26600306093` proved `/v1/health/simple`, `/dashboard`, and a bundled `/dashboard/assets/<hash>` response from the running container.

Evidence: `.planning/phases/08-ci-release-automation/08-03-DOCKER-DASHBOARD-PACKAGING.md`

### 08-04 — Release/tag automation guardrail

Added `.github/workflows/release.yml` as manual-only, read-only release verification. It requires explicit `version` and `confirm_no_publish=true`, checks `pyproject.toml` version equality, refuses existing tags, builds Python artifacts, runs `twine check`, builds dashboard and Docker image, and uploads artifacts without publishing.

Evidence: `.planning/phases/08-ci-release-automation/08-04-RELEASE-TAG-AUTOMATION-GUARDRAIL.md`

### 08-05 — Dependency drift guard

Added `scripts/check_dependency_drift.py`, unit tests, CI `dependency-drift` job, `.gsdrc.toml` verify command, and docs. The guard compares `requirements.txt` ↔ `pyproject.toml` and `web/package.json` ↔ `web/package-lock.json` root specs.

Evidence: `.planning/phases/08-ci-release-automation/08-05-DEPENDENCY-DRIFT-GUARD.md`

## Local Verification Snapshot

Passed locally before push:

- Full backend pytest: **249 passed / 9 skipped**
- Dependency drift guard: **passed** (`25` backend pins, `29` pyproject deps, `28` frontend deps, `24` frontend devDeps)
- `actionlint` for `ci.yml` + `release.yml`: **passed**
- Workflow run-step `bash -n`: **26 run steps passed**
- `git diff --check`: **passed**
- GSD health: **healthy**
- GSD consistency: **passed with 6 known non-blocking warnings**
- `docker compose --env-file .env.example config`: **passed**
- Frontend audit/lint/typecheck/tests/build: **passed** (`42` Vitest tests)
- Python package build + `twine check`: **passed**

## Active Next Step

1. Stage changed files.
2. Run changed-file secret scan.
3. Commit and push.
4. Verify `HEAD = origin/master = remote/master`.
5. Inspect GitHub Actions run for the pushed commit.
6. If CI passes, update Phase 08 closeout evidence and mark Phase 08 complete.

## Fixed Guardrails

- No automatic tag creation.
- No PyPI, Docker Hub, GHCR, or other registry publishing unless explicitly selected later.
- Release workflow remains dry-run/manual by default.
- Release version/tag creation remains an operator decision.
