---
phase: 04-command-center-ui
plan: 05
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - web/src/routes/_authed/index.tsx
  - web/src/routes/_authed/agents/index.tsx
  - web/src/routes/_authed/agents/$agentId.tsx
  - web/src/routes/_authed/tasks/index.tsx
  - web/src/routes/_authed/tasks/$taskId.tsx
  - web/src/routes/_authed/workflows/index.tsx
  - web/src/routes/_authed/workflows/$workflowId.tsx
  - web/src/components/forms/TaskCreateForm.tsx
  - web/src/components/forms/TaskCreateForm.test.tsx
  - web/src/components/common/ResponsiveList.tsx
  - web/src/components/common/ResponsiveList.test.tsx
  - web/src/components/common/StatusBadge.tsx
  - web/src/components/common/TraceTimeline.tsx
  - web/src/components/common/TraceTimeline.test.tsx
  - web/src/locales/en/operations.json
  - web/src/locales/tr/operations.json
  - web/src/components/ui/badge.tsx
  - web/src/components/ui/dialog.tsx
  - web/src/components/ui/alert-dialog.tsx
  - web/src/components/ui/table.tsx
  - web/src/components/ui/select.tsx
  - web/src/components/ui/textarea.tsx
autonomous: true
requirements: ["UI-02", "UI-03", "UI-04", "UI-05", "UI-06", "UI-07", "UI-12", "UI-15"]
must_haves:
  truths:
    - "Agents list route renders at /agents with table on md+ and cards on <md"
    - "Agent detail /agents/$agentId shows capabilities, current task, last heartbeat"
    - "Tasks list /tasks filterable by status, real-time updates via WS merge"
    - "Task create dialog uses react-hook-form + zod, calls useCreateTask mutation"
    - "Task cancel uses AlertDialog confirmation with exact UI-SPEC copy, calls useCancelTask"
    - "Task detail /tasks/$taskId renders TraceTimeline (UI-12) when trace data present"
    - "Workflows list and workflow detail /workflows/$workflowId render step-list with status badges"
    - "All status badges use exact color tokens from UI-SPEC (emerald for online/completed, amber idle, red failed, zinc offline, sky running pulse, violet claimed)"
    - "ResponsiveList: table on md+ (hidden md:block), card stack on <md (md:hidden)"
  artifacts:
    - path: web/src/routes/_authed/agents/index.tsx
      provides: "UI-02 live agent board"
    - path: web/src/routes/_authed/tasks/index.tsx
      provides: "UI-03 task list + UI-04 create dialog + UI-05 cancel action"
    - path: web/src/components/common/TraceTimeline.tsx
      provides: "UI-12 vertical trace timeline with color-coded categories"
    - path: web/src/components/common/ResponsiveList.tsx
      provides: "UI-15 responsive table-to-card pattern"
  key_links:
    - from: web/src/routes/_authed/tasks/index.tsx
      to: web/src/hooks/queries/useTasks.ts
      via: "useTasks, useCreateTask, useCancelTask"
      pattern: "useTasks|useCreateTask|useCancelTask"
    - from: web/src/routes/_authed/agents/index.tsx
      to: web/src/hooks/queries/useAgents.ts
      via: "useAgents()"
      pattern: "useAgents\\(\\)"
    - from: web/src/components/forms/TaskCreateForm.tsx
      to: web/src/hooks/queries/useAgents.ts
      via: "agent selector dropdown"
      pattern: "useAgents"
    - from: web/src/components/common/StatusBadge.tsx
      to: "UI-SPEC status color map"
      via: "emerald-500|amber-500|red-500|zinc-500|sky-400|violet-400"
      pattern: "emerald-500|amber-500|red-500"
---

<objective>
Build the Operations feature routes: Agents (UI-02, UI-07), Tasks (UI-03, UI-04, UI-05, UI-12), Workflows (UI-06). Uses shared primitives ResponsiveList (UI-15) and StatusBadge for consistent status rendering. Task detail route renders TraceTimeline component implementing the UI-SPEC trace viewer spec.

Purpose: Ship the "operate the hub" surface. Users can see live agents, dispatch tasks, cancel running tasks, inspect workflow progress, and drill into task traces.

Output: `/agents`, `/agents/{id}`, `/tasks`, `/tasks/{id}`, `/workflows`, `/workflows/{id}` all work in dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-command-center-ui/04-CONTEXT.md
@.planning/phases/04-command-center-ui/04-UI-SPEC.md
@.planning/phases/04-command-center-ui/04-02-SUMMARY.md
@.planning/phases/04-command-center-ui/04-03-SUMMARY.md
@.planning/phases/04-command-center-ui/04-04-SUMMARY.md

<interfaces>
From Plan 04 (data layer):
- useAgents() → UseQueryResult<Agent[]>
- useAgent(id) → UseQueryResult<Agent>
- useTasks(filters?) → UseQueryResult<Task[]>
- useTask(id) → UseQueryResult<Task>
- useCreateTask() → UseMutationResult<Task, Error, CreateTaskPayload>
- useCancelTask() → UseMutationResult<Task, Error, string>
- useWorkflows() / useWorkflow(id)
- types from web/src/types/entities.ts: Agent, Task, Workflow, WorkflowStep, AgentStatus, TaskStatus

Status color tokens (UI-SPEC § Color § Status color map):
Agent: online=emerald-500, idle=amber-500, offline=zinc-500, error=red-500 (dot + badge bg/10 + text-400)
Task: queued=zinc-400, claimed=violet-400, running=sky-400 (animate-pulse), completed=emerald-500, failed=red-500, cancelled=zinc-500 strikethrough

Trace viewer (UI-SPEC lines 270-286): vertical timeline, 16px per level indent (pl-4), category colors: llm=violet-400, tool=sky-400, db=amber-400, http=emerald-400, internal=zinc-500, error=red-500, duration bar proportional width.

Copy table strings (UI-SPEC lines 164-206) — MUST come from locales, no inline English.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install feature shadcn primitives + locales + shared StatusBadge/ResponsiveList/TraceTimeline components</name>
  <files>web/src/components/ui/badge.tsx, web/src/components/ui/dialog.tsx, web/src/components/ui/alert-dialog.tsx, web/src/components/ui/table.tsx, web/src/components/ui/select.tsx, web/src/components/ui/textarea.tsx, web/src/components/common/StatusBadge.tsx, web/src/components/common/ResponsiveList.tsx, web/src/components/common/ResponsiveList.test.tsx, web/src/components/common/TraceTimeline.tsx, web/src/components/common/TraceTimeline.test.tsx, web/src/locales/en/operations.json, web/src/locales/tr/operations.json</files>
  <read_first>
    - .planning/phases/04-command-center-ui/04-UI-SPEC.md (Status color map lines 126-155, Trace viewer lines 270-286, Copy table agents/tasks/workflows entries, Mobile responsive lines 263-269, ResponsiveList pattern in 04-RESEARCH.md lines 837-870)
    - web/components.json
  </read_first>
  <action>
Install shadcn primitives:
```bash
cd /home/omer/projects/OpenHub/web
npx shadcn@latest add badge -y
npx shadcn@latest add dialog -y
npx shadcn@latest add alert-dialog -y
npx shadcn@latest add table -y
npx shadcn@latest add select -y
npx shadcn@latest add textarea -y
```

Write `web/src/locales/en/operations.json`:
```json
{
  "agents": {
    "title": "Agents",
    "columns": { "name": "Name", "status": "Status", "capabilities": "Capabilities", "lastSeen": "Last seen", "currentTask": "Current task" },
    "emptyHeading": "No agents registered",
    "emptyBody": "Connect an agent via scripts/run_bridge.py or POST /v1/agents.",
    "status": { "online": "online", "offline": "offline", "idle": "idle", "error": "error" }
  },
  "tasks": {
    "title": "Tasks",
    "createCta": "Create task",
    "dialogTitle": "Create task",
    "dispatchCta": "Dispatch task",
    "cancelCta": "Cancel task",
    "cancelConfirmBody": "Cancel task: This will stop the running task. The agent will receive a cancel signal. Continue?",
    "columns": { "title": "Title", "status": "Status", "agent": "Agent", "priority": "Priority", "updated": "Updated" },
    "filterAll": "All",
    "emptyHeading": "No tasks yet",
    "emptyBody": "Click Create task to dispatch your first task.",
    "fields": { "title": "Title", "description": "Description", "priority": "Priority", "agent": "Agent", "capabilities": "Required capabilities" },
    "status": { "queued": "queued", "claimed": "claimed", "running": "running", "completed": "completed", "failed": "failed", "cancelled": "cancelled" }
  },
  "workflows": {
    "title": "Workflows",
    "emptyHeading": "No workflows running",
    "emptyBody": "Workflows appear here when an agent dispatches one via the coordination API.",
    "columns": { "name": "Name", "status": "Status", "steps": "Steps", "updated": "Updated" }
  },
  "trace": {
    "emptyHeading": "Select a task to view its trace",
    "emptyBody": "Distributed traces show tool calls, sub-steps, and timing."
  }
}
```

Write `web/src/locales/tr/operations.json`:
```json
{
  "agents": {
    "title": "Ajanlar",
    "columns": { "name": "Ad", "status": "Durum", "capabilities": "Yetenekler", "lastSeen": "Son gorulme", "currentTask": "Aktif gorev" },
    "emptyHeading": "Kayitli ajan yok",
    "emptyBody": "scripts/run_bridge.py ile bir ajan bagla veya POST /v1/agents cagir.",
    "status": { "online": "online", "offline": "offline", "idle": "idle", "error": "hata" }
  },
  "tasks": {
    "title": "Gorevler",
    "createCta": "Gorev olustur",
    "dialogTitle": "Gorev olustur",
    "dispatchCta": "Gorevi gonder",
    "cancelCta": "Gorevi iptal et",
    "cancelConfirmBody": "Gorevi iptal et: Bu, calisan gorevi durdurur. Ajan iptal sinyali alir. Devam?",
    "columns": { "title": "Baslik", "status": "Durum", "agent": "Ajan", "priority": "Oncelik", "updated": "Guncellendi" },
    "filterAll": "Tumu",
    "emptyHeading": "Henuz gorev yok",
    "emptyBody": "Ilk gorevini olusturmak icin Gorev olustur'a tikla.",
    "fields": { "title": "Baslik", "description": "Aciklama", "priority": "Oncelik", "agent": "Ajan", "capabilities": "Gerekli yetenekler" },
    "status": { "queued": "kuyrukta", "claimed": "alindi", "running": "calisiyor", "completed": "tamamlandi", "failed": "basarisiz", "cancelled": "iptal" }
  },
  "workflows": {
    "title": "Workflow'lar",
    "emptyHeading": "Calisan workflow yok",
    "emptyBody": "Bir ajan koordinasyon API'si uzerinden bir workflow baslattiginda burada gorunur.",
    "columns": { "name": "Ad", "status": "Durum", "steps": "Adimlar", "updated": "Guncellendi" }
  },
  "trace": {
    "emptyHeading": "Trace'i gormek icin bir gorev sec",
    "emptyBody": "Dagitik trace'ler arac cagrilarini, alt adimlari ve zamanlamayi gosterir."
  }
}
```

Register the new namespace in `web/src/i18n/index.ts`. Read the existing file, then add:
```ts
import enOperations from '@/locales/en/operations.json'
import trOperations from '@/locales/tr/operations.json'
```
and extend the `ns` array to `['common', 'nav', 'operations']`, and extend each language object: `en: { common: enCommon, nav: enNav, operations: enOperations }`, `tr: { ... operations: trOperations }`.

Write `web/src/components/common/StatusBadge.tsx`:
```tsx
import { cn } from '@/lib/utils'
import type { AgentStatus, TaskStatus } from '@/types/entities'

// UI-SPEC status color map (verbatim tokens).
const agentTokens: Record<AgentStatus, { dot: string; bg: string; text: string }> = {
  online: { dot: 'bg-emerald-500', bg: 'bg-emerald-500/10', text: 'text-emerald-400' },
  idle: { dot: 'bg-amber-500', bg: 'bg-amber-500/10', text: 'text-amber-400' },
  offline: { dot: 'bg-zinc-500', bg: 'bg-zinc-500/10', text: 'text-zinc-400' },
  error: { dot: 'bg-red-500', bg: 'bg-red-500/10', text: 'text-red-400' },
}

const taskTokens: Record<TaskStatus, { dot: string; text: string; pulse?: boolean; strike?: boolean }> = {
  queued: { dot: 'bg-zinc-400', text: 'text-zinc-400' },
  claimed: { dot: 'bg-violet-400', text: 'text-violet-400' },
  running: { dot: 'bg-sky-400', text: 'text-sky-400', pulse: true },
  completed: { dot: 'bg-emerald-500', text: 'text-emerald-400' },
  failed: { dot: 'bg-red-500', text: 'text-red-400' },
  cancelled: { dot: 'bg-zinc-500', text: 'text-zinc-500', strike: true },
}

export function AgentStatusBadge({ status, label }: { status: AgentStatus; label: string }) {
  const tok = agentTokens[status]
  return (
    <span className={cn('inline-flex items-center gap-1.5 rounded-md px-2 py-0.5 text-xs font-medium', tok.bg, tok.text)}>
      <span className={cn('size-1.5 rounded-full', tok.dot)} />
      {label}
    </span>
  )
}

export function TaskStatusBadge({ status, label }: { status: TaskStatus; label: string }) {
  const tok = taskTokens[status]
  return (
    <span className={cn('inline-flex items-center gap-1.5 text-xs font-medium', tok.text, tok.strike && 'line-through')}>
      <span className={cn('size-1.5 rounded-full', tok.dot, tok.pulse && 'animate-pulse')} />
      {label}
    </span>
  )
}
```

Write `web/src/components/common/ResponsiveList.tsx`:
```tsx
import { ReactNode } from 'react'

interface Column<T> {
  key: keyof T | string
  label: string
  render?: (item: T) => ReactNode
}

interface Props<T extends { id: string }> {
  items: T[]
  columns: Column<T>[]
  cardRender: (item: T) => ReactNode
  emptyState?: ReactNode
}

// UI-15: table on md+ (hidden md:block), cards on <md (md:hidden).
// UI-SPEC Data table density: py-2 px-3, text-zinc-400 header, hover:bg-zinc-800/50.
export function ResponsiveList<T extends { id: string }>({ items, columns, cardRender, emptyState }: Props<T>) {
  if (items.length === 0 && emptyState) return <>{emptyState}</>
  return (
    <>
      <div className="hidden md:block">
        <table className="w-full border-collapse">
          <thead>
            <tr className="border-b border-zinc-800">
              {columns.map((c) => (
                <th
                  key={String(c.key)}
                  className="px-3 py-2 text-left text-xs font-medium uppercase tracking-wide text-zinc-400"
                >
                  {c.label}
                </th>
              ))}
            </tr>
          </thead>
          <tbody>
            {items.map((i) => (
              <tr key={i.id} className="border-b border-zinc-800 hover:bg-zinc-800/50">
                {columns.map((c) => (
                  <td key={String(c.key)} className="px-3 py-2 text-sm text-zinc-50 tabular-nums">
                    {c.render ? c.render(i) : String((i as any)[c.key] ?? '')}
                  </td>
                ))}
              </tr>
            ))}
          </tbody>
        </table>
      </div>
      <div className="md:hidden space-y-2 px-4 py-2">
        {items.map((i) => (
          <div key={i.id} className="rounded-lg border border-zinc-800 bg-zinc-900 p-3">
            {cardRender(i)}
          </div>
        ))}
      </div>
    </>
  )
}
```

Write `web/src/components/common/ResponsiveList.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ResponsiveList } from './ResponsiveList'

interface Row { id: string; name: string }

describe('ResponsiveList', () => {
  it('renders table headers', () => {
    render(
      <ResponsiveList<Row>
        items={[{ id: '1', name: 'foo' }]}
        columns={[{ key: 'name', label: 'Name' }]}
        cardRender={(i) => <div>{i.name}</div>}
      />,
    )
    expect(screen.getByText('Name')).toBeInTheDocument()
  })

  it('renders rows from items', () => {
    render(
      <ResponsiveList<Row>
        items={[{ id: '1', name: 'foo' }, { id: '2', name: 'bar' }]}
        columns={[{ key: 'name', label: 'Name' }]}
        cardRender={(i) => <div>{i.name}</div>}
      />,
    )
    // Each name appears twice (once in desktop table, once in mobile card)
    expect(screen.getAllByText('foo')).toHaveLength(2)
  })

  it('renders empty state when items empty', () => {
    render(
      <ResponsiveList<Row>
        items={[]}
        columns={[{ key: 'name', label: 'Name' }]}
        cardRender={(i) => <div>{i.name}</div>}
        emptyState={<div>no rows</div>}
      />,
    )
    expect(screen.getByText('no rows')).toBeInTheDocument()
  })
})
```

Write `web/src/components/common/TraceTimeline.tsx`:
```tsx
import { cn } from '@/lib/utils'

export interface TraceSpan {
  id: string
  name: string
  category: 'llm' | 'tool' | 'db' | 'http' | 'internal' | 'error'
  duration_ms: number
  start_ms: number
  children?: TraceSpan[]
  attributes?: Record<string, unknown>
}

// UI-SPEC trace viewer spec: verbatim category color tokens
const categoryColors: Record<TraceSpan['category'], string> = {
  llm: 'violet-400',
  tool: 'sky-400',
  db: 'amber-400',
  http: 'emerald-400',
  internal: 'zinc-500',
  error: 'red-500',
}

interface NodeProps {
  span: TraceSpan
  depth: number
  rootDuration: number
}

function Node({ span, depth, rootDuration }: NodeProps) {
  const color = categoryColors[span.category]
  const widthPct = rootDuration > 0 ? (span.duration_ms / rootDuration) * 100 : 0
  return (
    <div>
      <div
        className="flex items-center gap-2 border-l-[3px] py-1"
        style={{ borderColor: `var(--tw-${color}, currentColor)`, paddingLeft: `${Math.min(depth, 6) * 16}px` }}
      >
        <span className={`size-2 rounded-full bg-${color}`} />
        <span className="flex-1 truncate text-sm text-zinc-50">{span.name}</span>
        <div className="relative h-2 w-40 rounded bg-zinc-800">
          <div className={`absolute left-0 top-0 h-full rounded bg-${color}/60`} style={{ width: `${widthPct}%` }} />
        </div>
        <span className="w-16 text-right font-mono text-xs text-zinc-400 tabular-nums">{span.duration_ms}ms</span>
      </div>
      {span.children?.map((child) => (
        <Node key={child.id} span={child} depth={depth + 1} rootDuration={rootDuration} />
      ))}
    </div>
  )
}

export function TraceTimeline({ root }: { root: TraceSpan | null }) {
  if (!root) return null
  return (
    <div className={cn('rounded-lg border border-zinc-800 bg-zinc-900 p-4 font-sans')}>
      <Node span={root} depth={0} rootDuration={root.duration_ms} />
    </div>
  )
}
```

Write `web/src/components/common/TraceTimeline.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { TraceTimeline, type TraceSpan } from './TraceTimeline'

const sample: TraceSpan = {
  id: 'root',
  name: 'task.execute',
  category: 'internal',
  duration_ms: 1000,
  start_ms: 0,
  children: [
    { id: 'c1', name: 'llm.chat', category: 'llm', duration_ms: 400, start_ms: 100 },
    { id: 'c2', name: 'db.query', category: 'db', duration_ms: 150, start_ms: 500 },
  ],
}

describe('TraceTimeline', () => {
  it('renders root span name', () => {
    render(<TraceTimeline root={sample} />)
    expect(screen.getByText('task.execute')).toBeInTheDocument()
  })

  it('renders nested children', () => {
    render(<TraceTimeline root={sample} />)
    expect(screen.getByText('llm.chat')).toBeInTheDocument()
    expect(screen.getByText('db.query')).toBeInTheDocument()
  })

  it('renders nothing when root is null', () => {
    const { container } = render(<TraceTimeline root={null} />)
    expect(container.firstChild).toBeNull()
  })
})
```
  </action>
  <verify>
    <automated>cd /home/omer/projects/OpenHub/web &amp;&amp; npm run test -- --run src/components/common/ResponsiveList.test.tsx src/components/common/TraceTimeline.test.tsx &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `web/src/components/ui/{badge,dialog,alert-dialog,table,select,textarea}.tsx`
    - `web/src/components/common/StatusBadge.tsx` contains `emerald-500` AND `amber-500` AND `red-500` AND `zinc-500` AND `sky-400` AND `violet-400`
    - `web/src/components/common/ResponsiveList.tsx` contains `hidden md:block` AND `md:hidden`
    - `web/src/components/common/TraceTimeline.tsx` contains all 6 category colors (violet-400, sky-400, amber-400, emerald-400, zinc-500, red-500)
    - `web/src/locales/en/operations.json` contains `"cancelConfirmBody"`
    - `web/src/locales/tr/operations.json` contains `"Gorevi iptal et"`
    - `web/src/i18n/index.ts` contains `operations` namespace
    - `cd web && npm run test -- --run` exits 0 with ResponsiveList + TraceTimeline tests passing
    - `cd web && npm run typecheck` exits 0
  </acceptance_criteria>
  <done>
Shared components + primitives + locales ready for feature routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Agents routes (list + detail) with live updates and responsive layout</name>
  <files>web/src/routes/_authed/index.tsx, web/src/routes/_authed/agents/index.tsx, web/src/routes/_authed/agents/$agentId.tsx</files>
  <read_first>
    - web/src/hooks/queries/useAgents.ts
    - web/src/components/common/ResponsiveList.tsx
    - web/src/components/common/StatusBadge.tsx
    - .planning/phases/04-command-center-ui/04-UI-SPEC.md (Copy table for empty state, Typography page title 24px)
  </read_first>
  <action>
Write `web/src/routes/_authed/index.tsx` (root authenticated landing — redirects to /agents):
```tsx
import { createFileRoute, redirect } from '@tanstack/react-router'

export const Route = createFileRoute('/_authed/')({
  beforeLoad: () => {
    throw redirect({ to: '/agents' as any })
  },
})
```

Write `web/src/routes/_authed/agents/index.tsx`:
```tsx
import { createFileRoute, Link } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useAgents } from '@/hooks/queries/useAgents'
import { ResponsiveList } from '@/components/common/ResponsiveList'
import { AgentStatusBadge } from '@/components/common/StatusBadge'
import type { Agent } from '@/types/entities'

export const Route = createFileRoute('/_authed/agents/')({
  component: AgentsList,
})

function AgentsList() {
  const { t } = useTranslation('operations')
  const { data: agents = [], isLoading } = useAgents()

  if (isLoading) {
    return <div className="p-6 text-sm text-zinc-400">Loading agents...</div>
  }

  return (
    <div className="p-6">
      <h1 className="mb-4 text-2xl font-semibold text-zinc-50">{t('agents.title')}</h1>
      <ResponsiveList<Agent>
        items={agents}
        columns={[
          {
            key: 'name',
            label: t('agents.columns.name'),
            render: (a) => (
              <Link to={'/agents/$agentId' as any} params={{ agentId: a.id }} className="text-zinc-50 hover:text-emerald-500">
                {a.name}
              </Link>
            ),
          },
          {
            key: 'status',
            label: t('agents.columns.status'),
            render: (a) => <AgentStatusBadge status={a.status} label={t(`agents.status.${a.status}`)} />,
          },
          {
            key: 'capabilities',
            label: t('agents.columns.capabilities'),
            render: (a) => <span className="font-mono text-xs text-zinc-400">{a.capabilities.join(', ')}</span>,
          },
          {
            key: 'last_heartbeat',
            label: t('agents.columns.lastSeen'),
            render: (a) => <span className="font-mono text-xs text-zinc-400">{a.last_heartbeat ?? '-'}</span>,
          },
        ]}
        cardRender={(a) => (
          <div className="flex flex-col gap-1">
            <div className="flex items-center justify-between">
              <Link to={'/agents/$agentId' as any} params={{ agentId: a.id }} className="font-semibold text-zinc-50">
                {a.name}
              </Link>
              <AgentStatusBadge status={a.status} label={t(`agents.status.${a.status}`)} />
            </div>
            <div className="text-xs text-zinc-400">{a.capabilities.join(', ')}</div>
          </div>
        )}
        emptyState={
          <div className="p-6 text-center">
            <div className="text-lg font-semibold text-zinc-50">{t('agents.emptyHeading')}</div>
            <div className="mt-2 text-sm text-zinc-400">{t('agents.emptyBody')}</div>
          </div>
        }
      />
    </div>
  )
}
```

Write `web/src/routes/_authed/agents/$agentId.tsx`:
```tsx
import { createFileRoute } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useAgent } from '@/hooks/queries/useAgents'
import { AgentStatusBadge } from '@/components/common/StatusBadge'

export const Route = createFileRoute('/_authed/agents/$agentId')({
  component: AgentDetail,
})

function AgentDetail() {
  const { agentId } = Route.useParams()
  const { t } = useTranslation('operations')
  const { data: agent, isLoading } = useAgent(agentId)

  if (isLoading) return <div className="p-6 text-sm text-zinc-400">Loading...</div>
  if (!agent) return <div className="p-6 text-sm text-zinc-400">Agent not found</div>

  return (
    <div className="p-6">
      <div className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-zinc-50">{agent.name}</h1>
        <AgentStatusBadge status={agent.status} label={t(`agents.status.${agent.status}`)} />
      </div>
      <dl className="grid grid-cols-2 gap-4 text-sm">
        <div>
          <dt className="text-xs uppercase tracking-wide text-zinc-400">ID</dt>
          <dd className="font-mono text-zinc-50">{agent.id}</dd>
        </div>
        <div>
          <dt className="text-xs uppercase tracking-wide text-zinc-400">{t('agents.columns.lastSeen')}</dt>
          <dd className="font-mono text-zinc-50">{agent.last_heartbeat ?? '-'}</dd>
        </div>
        <div className="col-span-2">
          <dt className="text-xs uppercase tracking-wide text-zinc-400">{t('agents.columns.capabilities')}</dt>
          <dd className="font-mono text-zinc-50">{agent.capabilities.join(', ')}</dd>
        </div>
        <div className="col-span-2">
          <dt className="text-xs uppercase tracking-wide text-zinc-400">{t('agents.columns.currentTask')}</dt>
          <dd className="font-mono text-zinc-50">{agent.current_task_id ?? '-'}</dd>
        </div>
      </dl>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /home/omer/projects/OpenHub/web &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/routes/_authed/agents/index.tsx` contains `useAgents` AND `ResponsiveList` AND `AgentStatusBadge`
    - File `web/src/routes/_authed/agents/$agentId.tsx` contains `Route.useParams()` AND `useAgent(agentId)`
    - File `web/src/routes/_authed/index.tsx` redirects to `/agents`
    - `cd web && npm run typecheck` exits 0
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>
Agents list + detail routes shipped with live WS updates and responsive layout.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tasks routes (list + create dialog + cancel action + detail with TraceTimeline) + Workflows routes</name>
  <files>web/src/routes/_authed/tasks/index.tsx, web/src/routes/_authed/tasks/$taskId.tsx, web/src/routes/_authed/workflows/index.tsx, web/src/routes/_authed/workflows/$workflowId.tsx, web/src/components/forms/TaskCreateForm.tsx, web/src/components/forms/TaskCreateForm.test.tsx</files>
  <behavior>
    - TaskCreateForm: zod schema {title: min(1), description: optional, priority: int 1-5, agent_id: optional}
    - TaskCreateForm: submit calls useCreateTask mutation, success toast + close dialog
    - TaskCreateForm.test: render form, fill title + priority, submit, verify useCreateTask mutation fired with payload
    - Tasks list: filter chips for status (using Select), ResponsiveList with TaskStatusBadge per row
    - Tasks list: per-row Cancel button for running/queued/claimed, opens AlertDialog with exact cancelConfirmBody copy, confirm calls useCancelTask
    - Task detail: shows Task fields + TraceTimeline (currently renders empty state since backend trace endpoint TBD)
    - Workflows list: uses useWorkflows + ResponsiveList
    - Workflow detail: uses useWorkflow(id), renders steps list with status badges
  </behavior>
  <read_first>
    - web/src/hooks/queries/useTasks.ts
    - web/src/hooks/queries/useWorkflows.ts
    - web/src/components/ui/dialog.tsx
    - web/src/components/ui/alert-dialog.tsx
    - web/src/components/ui/select.tsx
    - web/src/components/common/TraceTimeline.tsx
    - .planning/phases/04-command-center-ui/04-UI-SPEC.md (Destructive actions lines 207-212, Copy table task entries)
  </read_first>
  <action>
Write `web/src/components/forms/TaskCreateForm.tsx`:
```tsx
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { useTranslation } from 'react-i18next'
import { toast } from 'sonner'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Textarea } from '@/components/ui/textarea'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form'
import { useCreateTask } from '@/hooks/queries/useTasks'
import { useAgents } from '@/hooks/queries/useAgents'
import { ApiError } from '@/lib/api-client'

const schema = z.object({
  title: z.string().min(1, 'Title required'),
  description: z.string().optional(),
  priority: z.coerce.number().int().min(1).max(5).default(3),
  agent_id: z.string().optional().nullable(),
})

type Values = z.infer<typeof schema>

export function TaskCreateForm({ onDone }: { onDone?: () => void }) {
  const { t } = useTranslation(['operations', 'common'])
  const createTask = useCreateTask()
  const { data: agents = [] } = useAgents()

  const form = useForm<Values>({
    resolver: zodResolver(schema),
    defaultValues: { title: '', description: '', priority: 3, agent_id: null },
  })

  const onSubmit = async (v: Values) => {
    try {
      await createTask.mutateAsync({
        title: v.title,
        description: v.description,
        priority: v.priority,
        agent_id: v.agent_id || null,
      })
      form.reset()
      onDone?.()
    } catch (err) {
      if (err instanceof ApiError) {
        toast.error(err.problem.title, { description: err.problem.detail })
      } else {
        toast.error(t('common:requestFailed'))
      }
    }
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="flex flex-col gap-4">
        <FormField
          control={form.control}
          name="title"
          render={({ field }) => (
            <FormItem>
              <FormLabel>{t('operations:tasks.fields.title')}</FormLabel>
              <FormControl>
                <Input {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        <FormField
          control={form.control}
          name="description"
          render={({ field }) => (
            <FormItem>
              <FormLabel>{t('operations:tasks.fields.description')}</FormLabel>
              <FormControl>
                <Textarea rows={3} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        <FormField
          control={form.control}
          name="priority"
          render={({ field }) => (
            <FormItem>
              <FormLabel>{t('operations:tasks.fields.priority')}</FormLabel>
              <FormControl>
                <Input type="number" min={1} max={5} {...field} />
              </FormControl>
              <FormMessage />
            </FormItem>
          )}
        />
        <FormField
          control={form.control}
          name="agent_id"
          render={({ field }) => (
            <FormItem>
              <FormLabel>{t('operations:tasks.fields.agent')}</FormLabel>
              <Select onValueChange={field.onChange} value={field.value ?? undefined}>
                <FormControl>
                  <SelectTrigger>
                    <SelectValue placeholder="-" />
                  </SelectTrigger>
                </FormControl>
                <SelectContent>
                  {agents.map((a) => (
                    <SelectItem key={a.id} value={a.id}>
                      {a.name}
                    </SelectItem>
                  ))}
                </SelectContent>
              </Select>
              <FormMessage />
            </FormItem>
          )}
        />
        <Button type="submit" disabled={createTask.isPending}>
          {t('operations:tasks.dispatchCta')}
        </Button>
      </form>
    </Form>
  )
}
```

Write `web/src/components/forms/TaskCreateForm.test.tsx`:
```tsx
import { describe, it, expect } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { http, HttpResponse } from 'msw'
import React from 'react'
import { server } from '@/mocks/server'
import { TaskCreateForm } from './TaskCreateForm'
import '@/i18n'

function wrap() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return ({ children }: { children: React.ReactNode }) =>
    React.createElement(QueryClientProvider, { client: qc }, children)
}

describe('TaskCreateForm', () => {
  it('submits valid payload to /v1/tasks', async () => {
    let receivedBody: any = null
    server.use(
      http.get('/v1/agents', () => HttpResponse.json([])),
      http.post('/v1/tasks', async ({ request }) => {
        receivedBody = await request.json()
        return HttpResponse.json({
          id: 't1',
          title: receivedBody.title,
          status: 'queued',
          priority: receivedBody.priority,
          created_at: '',
          updated_at: '',
        })
      }),
    )

    render(<TaskCreateForm />, { wrapper: wrap() })
    const user = userEvent.setup()

    await user.type(await screen.findByLabelText(/title|baslik/i), 'Do the thing')
    await user.click(screen.getByRole('button', { name: /dispatch|gonder/i }))

    await waitFor(() => expect(receivedBody).not.toBeNull())
    expect(receivedBody.title).toBe('Do the thing')
    expect(receivedBody.priority).toBe(3)
  })
})
```

Write `web/src/routes/_authed/tasks/index.tsx`:
```tsx
import { useState } from 'react'
import { createFileRoute, Link } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useTasks, useCancelTask } from '@/hooks/queries/useTasks'
import { ResponsiveList } from '@/components/common/ResponsiveList'
import { TaskStatusBadge } from '@/components/common/StatusBadge'
import { Button } from '@/components/ui/button'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog'
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from '@/components/ui/alert-dialog'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { TaskCreateForm } from '@/components/forms/TaskCreateForm'
import type { Task, TaskStatus } from '@/types/entities'

export const Route = createFileRoute('/_authed/tasks/')({
  component: TasksList,
})

const ALL_STATUSES: TaskStatus[] = ['queued', 'claimed', 'running', 'completed', 'failed', 'cancelled']

function TasksList() {
  const { t } = useTranslation(['operations', 'common'])
  const [statusFilter, setStatusFilter] = useState<string>('')
  const { data: tasks = [] } = useTasks({ status: statusFilter || undefined })
  const cancelTask = useCancelTask()
  const [dialogOpen, setDialogOpen] = useState(false)

  const canCancel = (s: TaskStatus) => s === 'queued' || s === 'claimed' || s === 'running'

  return (
    <div className="p-6">
      <div className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-zinc-50">{t('operations:tasks.title')}</h1>
        <div className="flex items-center gap-2">
          <Select value={statusFilter} onValueChange={(v) => setStatusFilter(v === 'all' ? '' : v)}>
            <SelectTrigger className="w-40">
              <SelectValue placeholder={t('operations:tasks.filterAll')} />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="all">{t('operations:tasks.filterAll')}</SelectItem>
              {ALL_STATUSES.map((s) => (
                <SelectItem key={s} value={s}>
                  {t(`operations:tasks.status.${s}`)}
                </SelectItem>
              ))}
            </SelectContent>
          </Select>
          <Dialog open={dialogOpen} onOpenChange={setDialogOpen}>
            <DialogTrigger asChild>
              <Button>{t('operations:tasks.createCta')}</Button>
            </DialogTrigger>
            <DialogContent>
              <DialogHeader>
                <DialogTitle>{t('operations:tasks.dialogTitle')}</DialogTitle>
              </DialogHeader>
              <TaskCreateForm onDone={() => setDialogOpen(false)} />
            </DialogContent>
          </Dialog>
        </div>
      </div>

      <ResponsiveList<Task>
        items={tasks}
        columns={[
          {
            key: 'title',
            label: t('operations:tasks.columns.title'),
            render: (ta) => (
              <Link to={'/tasks/$taskId' as any} params={{ taskId: ta.id }} className="text-zinc-50 hover:text-emerald-500">
                {ta.title}
              </Link>
            ),
          },
          {
            key: 'status',
            label: t('operations:tasks.columns.status'),
            render: (ta) => <TaskStatusBadge status={ta.status} label={t(`operations:tasks.status.${ta.status}`)} />,
          },
          { key: 'agent_id', label: t('operations:tasks.columns.agent'), render: (ta) => <span className="font-mono text-xs">{ta.agent_id ?? '-'}</span> },
          { key: 'priority', label: t('operations:tasks.columns.priority') },
          {
            key: 'actions' as any,
            label: '',
            render: (ta) =>
              canCancel(ta.status) ? (
                <AlertDialog>
                  <AlertDialogTrigger asChild>
                    <Button size="sm" variant="destructive">
                      {t('operations:tasks.cancelCta')}
                    </Button>
                  </AlertDialogTrigger>
                  <AlertDialogContent>
                    <AlertDialogHeader>
                      <AlertDialogTitle>{t('operations:tasks.cancelCta')}</AlertDialogTitle>
                      <AlertDialogDescription>{t('operations:tasks.cancelConfirmBody')}</AlertDialogDescription>
                    </AlertDialogHeader>
                    <AlertDialogFooter>
                      <AlertDialogCancel>{t('common:signOut') /* reuse existing close label not ideal; use a dedicated key */}</AlertDialogCancel>
                      <AlertDialogAction
                        className="bg-red-500 hover:bg-red-600"
                        onClick={() => cancelTask.mutate(ta.id)}
                      >
                        {t('operations:tasks.cancelCta')}
                      </AlertDialogAction>
                    </AlertDialogFooter>
                  </AlertDialogContent>
                </AlertDialog>
              ) : null,
          },
        ]}
        cardRender={(ta) => (
          <div className="flex flex-col gap-2">
            <div className="flex items-center justify-between">
              <Link to={'/tasks/$taskId' as any} params={{ taskId: ta.id }} className="font-semibold text-zinc-50">
                {ta.title}
              </Link>
              <TaskStatusBadge status={ta.status} label={t(`operations:tasks.status.${ta.status}`)} />
            </div>
            {canCancel(ta.status) && (
              <Button size="sm" variant="destructive" onClick={() => cancelTask.mutate(ta.id)}>
                {t('operations:tasks.cancelCta')}
              </Button>
            )}
          </div>
        )}
        emptyState={
          <div className="p-6 text-center">
            <div className="text-lg font-semibold text-zinc-50">{t('operations:tasks.emptyHeading')}</div>
            <div className="mt-2 text-sm text-zinc-400">{t('operations:tasks.emptyBody')}</div>
          </div>
        }
      />
    </div>
  )
}
```

Write `web/src/routes/_authed/tasks/$taskId.tsx`:
```tsx
import { createFileRoute } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useTask } from '@/hooks/queries/useTasks'
import { TaskStatusBadge } from '@/components/common/StatusBadge'
import { TraceTimeline } from '@/components/common/TraceTimeline'

export const Route = createFileRoute('/_authed/tasks/$taskId')({
  component: TaskDetail,
})

function TaskDetail() {
  const { taskId } = Route.useParams()
  const { t } = useTranslation('operations')
  const { data: task, isLoading } = useTask(taskId)

  if (isLoading) return <div className="p-6 text-sm text-zinc-400">Loading...</div>
  if (!task) return <div className="p-6 text-sm text-zinc-400">Task not found</div>

  return (
    <div className="p-6">
      <div className="mb-4 flex items-center justify-between">
        <h1 className="text-2xl font-semibold text-zinc-50">{task.title}</h1>
        <TaskStatusBadge status={task.status} label={t(`tasks.status.${task.status}`)} />
      </div>
      <dl className="mb-6 grid grid-cols-2 gap-4 text-sm">
        <div>
          <dt className="text-xs uppercase tracking-wide text-zinc-400">ID</dt>
          <dd className="font-mono text-zinc-50">{task.id}</dd>
        </div>
        <div>
          <dt className="text-xs uppercase tracking-wide text-zinc-400">Agent</dt>
          <dd className="font-mono text-zinc-50">{task.agent_id ?? '-'}</dd>
        </div>
      </dl>
      <h2 className="mb-2 text-lg font-semibold text-zinc-50">Trace</h2>
      <TraceTimeline root={null} />
      <div className="mt-2 text-center text-sm text-zinc-400">
        <div>{t('trace.emptyHeading')}</div>
        <div>{t('trace.emptyBody')}</div>
      </div>
    </div>
  )
}
```

Write `web/src/routes/_authed/workflows/index.tsx`:
```tsx
import { createFileRoute, Link } from '@tanstack/react-router'
import { useTranslation } from 'react-i18next'
import { useWorkflows } from '@/hooks/queries/useWorkflows'
import { ResponsiveList } from '@/components/common/ResponsiveList'
import type { Workflow } from '@/types/entities'

export const Route = createFileRoute('/_authed/workflows/')({
  component: WorkflowsList,
})

function WorkflowsList() {
  const { t } = useTranslation('operations')
  const { data: workflows = [] } = useWorkflows()

  return (
    <div className="p-6">
      <h1 className="mb-4 text-2xl font-semibold text-zinc-50">{t('workflows.title')}</h1>
      <ResponsiveList<Workflow>
        items={workflows}
        columns={[
          {
            key: 'name',
            label: t('workflows.columns.name'),
            render: (w) => (
              <Link to={'/workflows/$workflowId' as any} params={{ workflowId: w.id }} className="text-zinc-50 hover:text-emerald-500">
                {w.name}
              </Link>
            ),
          },
          { key: 'status', label: t('workflows.columns.status') },
          { key: 'steps', label: t('workflows.columns.steps'), render: (w) => String(w.steps.length) },
          { key: 'updated_at', label: t('workflows.columns.updated') },
        ]}
        cardRender={(w) => (
          <div className="flex items-center justify-between">
            <Link to={'/workflows/$workflowId' as any} params={{ workflowId: w.id }} className="font-semibold text-zinc-50">
              {w.name}
            </Link>
            <span className="text-xs text-zinc-400">{w.status}</span>
          </div>
        )}
        emptyState={
          <div className="p-6 text-center">
            <div className="text-lg font-semibold text-zinc-50">{t('workflows.emptyHeading')}</div>
            <div className="mt-2 text-sm text-zinc-400">{t('workflows.emptyBody')}</div>
          </div>
        }
      />
    </div>
  )
}
```

Write `web/src/routes/_authed/workflows/$workflowId.tsx`:
```tsx
import { createFileRoute } from '@tanstack/react-router'
import { useWorkflow } from '@/hooks/queries/useWorkflows'
import { Badge } from '@/components/ui/badge'

export const Route = createFileRoute('/_authed/workflows/$workflowId')({
  component: WorkflowDetail,
})

function WorkflowDetail() {
  const { workflowId } = Route.useParams()
  const { data: workflow, isLoading } = useWorkflow(workflowId)

  if (isLoading) return <div className="p-6 text-sm text-zinc-400">Loading...</div>
  if (!workflow) return <div className="p-6 text-sm text-zinc-400">Workflow not found</div>

  return (
    <div className="p-6">
      <h1 className="mb-4 text-2xl font-semibold text-zinc-50">{workflow.name}</h1>
      <ol className="flex flex-col gap-2">
        {workflow.steps.map((step, idx) => (
          <li key={step.id} className="flex items-center gap-3 rounded-md border border-zinc-800 bg-zinc-900 p-3">
            <span className="font-mono text-xs text-zinc-400">{idx + 1}</span>
            <span className="flex-1 text-sm text-zinc-50">{step.name}</span>
            <Badge variant="outline">{step.status}</Badge>
          </li>
        ))}
      </ol>
    </div>
  )
}
```

Run gate:
```bash
cd /home/omer/projects/OpenHub/web
npm run typecheck
npm run test -- --run
npm run build
```

If some mock msw handlers don't exist for /v1/workflows or /v1/tasks with query, extend `web/src/mocks/handlers.ts` to return empty arrays. Do not break tests from other plans.
  </action>
  <verify>
    <automated>cd /home/omer/projects/OpenHub/web &amp;&amp; npm run typecheck &amp;&amp; npm run test -- --run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `web/src/routes/_authed/tasks/index.tsx` contains `useCreateTask`, `useCancelTask`, `AlertDialog`, `cancelConfirmBody`
    - File `web/src/components/forms/TaskCreateForm.tsx` contains `useCreateTask` AND `zodResolver`
    - File `web/src/routes/_authed/tasks/$taskId.tsx` contains `TraceTimeline`
    - File `web/src/routes/_authed/workflows/index.tsx` contains `useWorkflows`
    - File `web/src/routes/_authed/workflows/$workflowId.tsx` contains `workflow.steps.map`
    - `cd web && npm run test -- --run src/components/forms/TaskCreateForm.test.tsx` exits 0
    - `cd web && npm run typecheck` exits 0
    - `cd web && npm run build` exits 0
  </acceptance_criteria>
  <done>
Operations feature group complete: agents, tasks (with create + cancel + trace), workflows all routed, tested, typed, built.
  </done>
</task>

</tasks>

<verification>
- `cd web && npm run typecheck` green
- `cd web && npm run test -- --run` green (ResponsiveList, TraceTimeline, TaskCreateForm tests pass alongside prior tests)
- `cd web && npm run build` green
- All route files exist under `web/src/routes/_authed/{agents,tasks,workflows}/`
</verification>

<success_criteria>
Navigating in dev to /agents, /agents/abc, /tasks, /tasks/abc, /workflows, /workflows/abc all render without crashing. Status badges use exact UI-SPEC tokens. Task create dialog dispatches tasks. Cancel action confirms then mutates. TraceTimeline component renders nested spans when data present.
</success_criteria>

<output>
Write `.planning/phases/04-command-center-ui/04-05-SUMMARY.md` with: routes shipped, status token table, forms tested, shadcn primitives added this plan.
</output>
